# Changelog

All notable changes to this package will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added (Toolbar `_` carry-over badge)
- New toolbar pill **`_ : Player (GameObject)`** surfaces the current `_` carry-over target so users can see at a glance whether the engine has a live value bound to `_` (the static the wrapper exposes inside snippets and Watch expressions). The badge appears whenever `ReplEngine.LastResult` is non-null and hides when it's null — Run that returns a non-null value, Object Browser inspect, and explicit `ReplEngine.SetLastResult` all light it up; an explicit Clear or `Reset Project Data` extinguishes it. Closes #59.
- Clicking the badge **re-inspects** the live `_` value: clears Output and renders the same `SimpleObjectSerializer.ToTree` shape Browse-inspect emits, so the user can fold the tree open and read the object's fields without typing `return _;` themselves. The summary chip reads `Inspect _` while in this mode so it's distinguishable from a Run or Browse landing. The badge's hover tooltip prints a concrete usage example (`return _;` / `_.someField`) so a newer user who's never seen the wrapper static doesn't have to guess what "available" means.
- A small **✕** sits inside the same pill and drops the carry-over via `ReplEngine.ResetLastResult()`. The clear routes through the engine (not directly at the badge) so every subscriber — this UI, future panels, the engine's own follow-up paths — sees the transition through one event.
- Object Browser inspect now emits an inline **`→ available as \`_\` in Code and Watch. Try: return _;`** info line right above the rendered tree. Without it users had to notice the toolbar badge separately to realise their next snippet / watch could reach the value through `_`; the inline mention plus a concrete invocation removes both the "available?" and the "available where?" ambiguity from the same glance as the inspection itself.

### Changed (`ReplEngine` carry-over plumbing)
- `ReplEngine.LastResult` mutations now route through a single private `AssignLastResult` chokepoint (`SetLastResult`, `ResetLastResult`, and the in-Execute success path all funnel through it). The new public `LastResultChanged` event fires from there with a `force` flag so explicit assignment (Set / successful Execute) notifies subscribers even when the new reference equals the current one — same-reference reassigns happen naturally when a snippet mutates and returns the same object (`_.name = "Hero"; return _;`) and the badge would otherwise freeze on the prior name. Reset keeps the `ReferenceEquals` short-circuit so a no-op `ResetLastResult()` on an already-null engine doesn't churn the UI.
- Subscribers are invoked through `GetInvocationList()` with a per-handler `try / catch`. A misbehaving UI surface that throws from `LastResultChanged` (a torn-down panel, a stale VisualElement) can no longer propagate up through `Execute`'s outer catch and convert a successful snippet into a `ReplResult.RuntimeError`. The carry-over field still updates and remaining subscribers still fire; the failure surfaces as a single `Debug.LogWarning` so the diagnostic isn't silent.

### Added (Object Browser row context menu)
- Object Browser rows now expose an explicit right-click menu — **Inspect**, **Set as `_`**, **Patch Method…**, **Copy Type Name**, **Copy Inspect Snippet** — so the panel reads as a set of action entry points instead of a list whose only contract is "double-click does *something* depending on mode." Closes #60.
- **Inspect** mirrors the Output-mode double-click flow: clears Output, renders `SimpleObjectSerializer.ToTree`, binds `_` to the value, refreshes Watch. **Patch Method…** mirrors the Patches-mode double-click flow (method picker for the row's runtime type, falls back to `DeclaredType` when the live value is null, e.g. singleton accessors). Both routes share the same helpers double-click drives, so the menu can't drift away from the existing behaviour.
- **Set as `_`** binds the value without rewriting Output — same `ReplEngine.SetLastResult(...)` the inspect path uses, just without the tree render. The toolbar `_` badge picks the new value up through `LastResultChanged` so the user still gets a visible confirmation, and the pane summary chip reads `Bound \`_\`` rather than `Browsed`.
- **Copy Type Name** writes the runtime type's `FullName` to the system clipboard (falls back to `DeclaredType` / display `TypeName` so singletons without a live value still produce something useful). **Copy Inspect Snippet** writes a small C# template into the clipboard, shaped by the row's category: MonoBehaviour rows get `return UnityEngine.Object.FindFirstObjectByType<T>();`, ScriptableObject rows get an `AssetDatabase.FindAssets("t:T")` / `LoadAssetAtPath<T>` chain (LINQ; assumes the user keeps `System.Linq` in Usings), and Singleton rows get a comment-only template with a TODO since the locator doesn't carry the accessor name (`.Instance` vs `.I` vs a property is project-specific). Both copy actions echo a one-line confirmation into Output so the click doesn't feel like a no-op.
- Menu entries that depend on a live instance (Inspect / Set as `_`) are greyed-out via `DropdownMenuAction.Status.Disabled` when the row's value resolved to null or a destroyed `UnityEngine.Object` — same liveness check the existing flow used inline. Patch Method stays enabled when the row has a `DeclaredType` even if the live value is gone, matching the singleton fallback.
- Double-click behaviour is unchanged. `ObjectBrowserView` exposes a parallel `OnRowAction` event in addition to the legacy `OnInstanceChosen`; host wires both, and the action handlers all reuse the same `RenderBrowserInspect` / `OpenBrowserPatchMethod` helpers the double-click path calls. A pooled `ContextualMenuManipulator` attached once in `MakeRow` walks up from the click target's `userData` to find the bound `InstanceEntry`, so the manipulator allocates the action lambdas at creation rather than per scroll-bind.

## [0.7.4] - 2026-05-18

### Added (Ctrl+F find overlay across Output / Watch / Patches)
- Press **Ctrl+F** (Cmd+F on macOS) anywhere in the REPL window to open a search bar above the toolbar. The bar scans:
  - **Output** panel — log lines + result trees (names + types + previews).
  - **Watch** panel — row expression / preview / type / error / source description, plus the inline expandable tree when the row is open. Collapsed Watch trees are skipped (expand a row first to make its nodes searchable).
  - **Patches** view — the active patches list (target type + method + parameter list + last error) *and* the live edit form: Type / Method / Params fields plus the multi-line **Body** editor (one hit per occurrence with a `(line N)` label so Next / Prev walks each match in turn). The body editor's parent `ScrollView` is driven directly via `scrollOffset` to bring the match line into the viewport, and a translucent yellow rectangle parented to the ScrollView's content container is positioned over the match line. The column offset measures prefix + match widths through a sentinel-wrapped `TextElement.MeasureTextSize` probe (`MeasureTextSize("X" + span + "X").x - MeasureTextSize("XX").x`) — wrapping the span in visible anchors moves the leading / trailing whitespace into the *interior* of the measured string, where Unity's edge-whitespace trim doesn't apply. Without that wrapper an indented line measured as if the leading spaces were zero pixels wide and the marker only covered half the match before running out of width. Form-field hits (Type / Method / Params) paint a thin warm accent border on the matching field via a CSS class. None of the Patches hits call `Focus()` / `SelectRange()` on a `TextField` — the previous focus-then-refocus dance kept stealing the keyboard from the Find input on the way back, which broke "type to refine the query." The marker is independent of focus and stays visible while the user keeps typing.
- Matching characters are highlighted in place with bold orange-yellow rich-text rather than tinting the whole row — the old full-row background fill is gone. A thin warm left-side accent marks the row currently focused under Next / Prev, so the user-visible cue is "where my eye is" without colouring everything that contains the query.
- Hits in the Patches view auto-flip the **Output / Patches** mode tab to **Patches** before scrolling — without that the pane would still be `display:none` under Output mode and the scroll would land somewhere invisible.
- Navigation is **Enter-driven**, code-editor style. Typing the query only updates the hit list + counter — *no* auto-scroll, *no* focus shift on each keystroke. **Enter** / **F3** jumps to the next hit; **Shift+Enter** / **Shift+F3** to previous; **Esc** closes. The counter reads `5 matches — Enter` before the first navigation and switches to `3 / 12` once you start stepping through. While the overlay is open, **F3 / Shift+F3 / Esc** are intercepted at the window root so they steer Find from anywhere — but **Enter is not** at the window level: if the user has clicked into the Patches body / code editor / Watch input, their Enter still inserts a newline / commits the field as normal. Enter only advances Find while focus is on the Find input itself, which is where it lands by default after Ctrl+F.
- The hit list refreshes automatically when any of the panels rebuilds (a Run finishes, the Watch evaluator refreshes, the patch registry mutates, the user edits the in-progress Patches form fields including the **Body**) so navigation never points at a disposed `VisualElement`. Source-driven rebuilds *preserve the current index* and re-apply scroll / marker against the updated content — type into the body after navigating and the marker shifts with the new match position instead of vanishing until the next Enter. Query changes still reset to "no selection" so the first Enter walks to hit #1.
- Implementation lives under `Editor/UI/Find/`: `ReplFindController` for state, `ReplFindOverlay` for the bar UI, `ReplFindHighlight` for the shared rich-text decorator (static query + per-label / per-tree subscription helpers, auto-cleanup on `DetachFromPanelEvent`), and a small `IReplFindable` interface implemented by `OutputFindable`, `WatchPanelView`, and `MethodPatchView`. Tree result panels stash a side-table `OutputTreeIndex` on `MultiColumnTreeView.userData` so the overlay can navigate to virtualized rows by id without re-walking the data on every keystroke.
- `Decorate()` wraps each user-text segment in `<noparse>…</noparse>` so REPL output containing markup-like substrings (`<b>Error</b>`, generic-syntax dumps like `List<int>`, XML/HTML-like log lines) stays intact when a query splices the highlight tags through it. Without the wrapper, searching for `b` inside `<b>Error</b>` would produce a string the UI Toolkit text engine couldn't parse and the row would render as garbage; with it the angle brackets render literally and only the matched character span flips bold/coloured.
- `ReplFindOverlay.Dispose()` now performs the same static-state cleanup `Hide()` does — clearing `ReplFindHighlight.ActiveQuery` (via `_controller.Close()`) and the `RequestRefocusInput` hook — before detaching its own event handlers. Without that, a CreateGUI rebuild or window close while the bar was open left the static query lingering: the next CreateGUI's `BindLabelText` would honour the stale query and paint matches against a bar that wasn't on screen, and the static delegate kept a managed reference to the disposed overlay (and through it the window root) blocking GC.

## [0.7.3] - 2026-05-12

### Changed (Korean README rewritten in natural prose)
- `README_kr.md` rewritten to read as native Korean rather than as a literal translation of `README.md`. Removed translationese tics (over-use of "…할 수 있습니다", chained "…됩니다" passive endings, "권장합니다 / 구성되어 있습니다" stiff phrasings, comma-heavy English-style enumerations), broke up multi-clause English sentences into shorter Korean ones, and softened the section headings ("왜 쓰나요?" → "어떤 일을 도와주나요"). Code blocks, menu paths, the helper table, and the meaning of every section are unchanged so the EN / KR pair still describes the same package; only the prose register moved.

### Removed (UsingsStore legacy key migration)
- `UsingsStore.LegacyKey` (`RoslynRepl.CustomUsings`, the project-agnostic key the pre-Phase-4 builds wrote to) and the `MigrateLegacyKeyIfNeeded` step that ran on the first `LoadCustom` / `Save` after each domain reload are gone. The package shipped publicly at 0.7.2; before that the migration window covered users still upgrading from local 0.7.0 / 0.7.1 builds, but those paths no longer have a real consumer. Carrying the migration forward only added a stateful first-call branch on every load and a tiny `_legacyMigrationChecked` flag to remember whether we'd already swept the legacy key this session — both dead weight on every fresh install.
- A 0.7.1 install upgraded to 0.7.3 will keep its custom usings (the per-project bucket lookup is unchanged); any value that still sat under the global `RoslynRepl.CustomUsings` key on first launch of an even older build is no longer rescued. Wipe by hand from EditorPrefs if you need to. `Reset Project Data` continues to clear the current per-project key.
- Drive-by sweep for similarly-dead compatibility paths: `MethodPatchView.OnPullOriginalClicked` no longer special-cases the `// Supported scope: void instance methods.` historic starter text (only the current `DefaultBody` and its prefix shape are recognised), and a stale "legacy" word in `RoslynReplWindow.OnBrowserInstanceChosen`'s comment was relabelled to "default" so the description matches the live code. The `;`-vs-`,` ParameterTypes fallback in `MethodPatchSpec.SplitParamTypes` and the folder-mismatch warning in `ReplPackagePaths` stay — they describe live code paths that are still load-bearing for fresh installs. Closes #53.

### Added (Per-row Delete in the Patches list)
- The Patches list now has a per-row **Delete** button next to **Load** / **Revert**. Reset Project Data was previously the only way to drop a draft from the registry — too broad for "I'm done with this one row", since it also wipes snippets, history, watches, and every other Patch the user is mid-debugging. The list section header is renamed from "Active patches" to plain "Patches" (Revert leaves Inactive drafts behind, so the section is no longer "active only"); the empty-state placeholder reads `(no patches)` accordingly.
- Delete shows a confirm dialog ("Delete this patch draft?") that calls out the destructive payload (the persisted body for that spec disappears) and adds an explicit auto-revert note when the row is currently Active. The handler reverts the Harmony detour first if installed, then calls `PatchRegistry.Remove(spec)` which persists the removal — so a domain reload won't resurrect a deleted draft. Reset Project Data remains the broad cleanup path. Closes #52.
- `PatchPersistence.Save` now deletes the on-disk file when the supplied spec list ends up empty after filtering (instead of writing `{"version":1,"items":[]}` to disk). Without this, deleting the last patch row through the new button left a zero-content file on disk: the Patches UI rendered empty, but `HasAny()` kept returning `true` and Reset Project Data fell through the "Nothing to clear" early-return into the stale-file cleanup branch. Centralising the empty case at the persistence layer covers every caller — Remove, Clear-via-registry, AddOrUpdate of a filtered-out spec — without each call site having to remember.
- `Save` returns `bool` and propagates the `UserSettingsStorage.Delete` success flag through `PatchRegistry.Persist` and a new `PatchRegistry.Remove(spec, out bool persistedOk)` overload. The per-row Delete handler now distinguishes a clean success ("Deleted: …") from the soft-failure case where in-memory removal worked but `patches.json` couldn't be deleted (locked / read-only) — a domain reload would otherwise resurrect the supposedly-deleted draft. Partial-failure status surfaces inline with a hint to check the Console for the file path.
- `PatchRegistry.Remove` now persists *before* mutating `_byKey` (build the post-removal snapshot, call `Save`, only then drop the key from the live dictionary). On a multi-row delete the previous shape mutated first and then wrote — if the write threw on a hard I/O failure (permissions, disk full, atomic-replace contention) the registry was missing a key the file still had, and a domain reload resurrected the supposedly-deleted spec. The Delete UI handler wraps the call in `try/catch` so the exception surfaces as a "delete failed, list unchanged" status with the underlying error logged to the Console.
- The Delete failure status now branches on whether the row was Active before the click. For an inactive draft "patch list unchanged" is accurate; for an active row the earlier `PatchEngine.Revert(spec)` already removed the detour and persisted Status = Inactive before the failing `Remove` ran, so the message instead says "the patch was already reverted (detour removed, row is now Inactive); re-Apply if you want it active again". Without the branch the UI would falsely reassure the user that Play Mode behaviour was unchanged when in fact it was already reverted. PR-review followups on #52.

## [0.7.2] - 2026-05-12

### Changed (Legacy package-id fallback)
- The literal `com.roslyn-repl` folder probe in `ReplPackagePaths.ResolvePackageRoot` is gone. Path resolution now relies entirely on `PackageInfo.FindForAssembly` — Unity resolves embedded packages by the `name` field in `package.json`, not the containing folder name, so forcing the folder to match the published id would have broken every legitimate embedded-dev case (variants / forks / checkouts under different folder names sharing the same id).
- `IsBundledRoslynAssemblyPath` now matches the bundled-DLL location through *both* `PackageInfo.assetPath` (AssetDatabase form, e.g. `Packages/com.youngchan.roslyn-repl`) **and** `PackageInfo.resolvedPath` (filesystem form, e.g. `…/Library/PackageCache/com.youngchan.roslyn-repl@0.7.2`). The first path covers embedded / direct-folder installs; the second covers OpenUPM and any other source where Unity stages the package under `Library/PackageCache/`. Without the resolvedPath leg, OpenUPM-installed copies — whose `Assembly.Location` never goes through the AssetDatabase form — would silently fall out of the BundledByUs classification and the setup verifier's "duplicate copy?" branch would lie about what's loaded.
- The match also tightened from two independent `Contains` checks (`/Editor/Plugins/Roslyn/` AND `/<root>`) to a single anchored segment `<root>/Editor/Plugins/Roslyn/` (with leading `/` boundary or path-start anchor), so a folder named `com.roslyn-repl-old/` no longer substring-matches a root of `com.roslyn-repl/`. PR-review followup.
- Soft nudge for embedded checkouts: when `PackageInfo.assetPath` ends in a folder name other than `com.youngchan.roslyn-repl`, the resolver emits a single `Debug.LogWarning` per editor session asking the developer to rename the folder before consuming via OpenUPM. Local development keeps working through PackageInfo in the meantime. Closes #45.

### Changed (Output result tree defaults to fields-only)
- The Output result tree no longer invokes user-defined property getters by default. A property getter can run lazy init, IO, log emission, or state mutation — inspecting a value in Output shouldn't trip any of that for the user. Watch already opted out for the same reason (refreshing every Run multiplies the side effects); Output now matches.
- New `Tools / Roslyn REPL / Output: Include Property Getters` toggle flips the behaviour per project. Off (default) walks fields only — instance, private, and inherited. On walks readable instance properties on top of that. The toggle lives in `RoslynRepl.Editor.Core.OutputSettings.IncludeProperties` so other surfaces can read or flip it programmatically.
- Existing `SimpleObjectSerializer.Options.IncludeProperties` default stays at `true` so direct callers don't quietly change behaviour; the Output panel routes through a new `BuildOutputTreeOptions()` helper that consults the setting. README updated. Closes #28.

### Changed (Apply-to-file backups moved out of Assets/Packages)
- `Apply to file` no longer drops `Foo.cs.bak` next to the edited `Foo.cs`. Unity used to import the backup as a regular asset (`.meta` generated, Project window pollution, easy to accidentally commit inside `Assets/` or `Packages/`). Backups now land under `<project>/Library/RoslynRepl/Backups/<yyyyMMdd_HHmmss_fff>_<name>.cs.bak` — `Library/` is the Unity-reserved derived-data folder that the asset importer skips and the stock `.gitignore` template excludes.
- The atomic-write pipeline is unchanged in spirit (same `File.Replace` swap with a same-directory dot-prefixed temp), only the destination of the pre-write backup moved. The Library/ path is passed to `File.Replace` as its `destinationBackupFileName` — Replace requires a non-null backup parameter, and the step-1 `File.Copy` already wrote identical bytes to that path, so Replace's overwrite is content-equivalent and the user's recovery copy stays valid before / during / after the swap. Source / destination / backup are all resolved to absolute filesystem paths first (`Path.GetFullPath` on the source) so `File.Replace` doesn't trip on the Unity-style asset paths it would otherwise see mixed with the absolute Library/ path; `AssetDatabase.ImportAsset` further down still receives the project-relative path it expects. Apply confirm dialog and the status bar message updated to point at the new path; README + README_kr Patches sections updated to call out the trade-off (Unity may delete `Library/` on a reimport — copy a backup out if you need long-term recovery). Closes #44.

### Changed (User data moved from EditorPrefs to project-local files)
- Patch bodies, snippets, run history, and watch expressions now live in `<project>/UserSettings/RoslynRepl/*.json` instead of `EditorPrefs`. On Windows EditorPrefs is registry-backed, so the historical store leaked beyond project deletion and bloated as patches and snippets grew. The new files tie the data to the project folder — deleting the project reclaims everything, and `UserSettings/` is the same place Unity ships its own per-user / per-project layout files.
- **Breaking** for any pre-release 0.7.1 install: there is no migration from the old EditorPrefs keys. The package has not had a public consumer release yet, so preserving the historical blob would only carry forward dead code on every Load. Any 0.7.1 EditorPrefs entry stays orphaned in the registry; remove it by hand if you need to (the keys started with `RoslynRepl.`).
- The first time the package writes into the folder it also drops a `UserSettings/RoslynRepl/.gitignore` containing a single `*` rule, so files in here never show up in `git status` — even on consumer projects whose top-level `.gitignore` doesn't already cover `UserSettings/`. This is defense-in-depth: patch bodies and history entries can carry server URLs / auth tokens / account values from a mid-debug paste, and a stock template gitignore is not a guarantee. Caught in PR review.
- New `RoslynRepl.Editor.Core.UserSettingsStorage` is the single I/O helper every store routes through. Writes go through the same atomic `File.Replace` pipeline the source export uses (write to a dot-prefixed temp in the destination directory, then swap) so a crash mid-save can't leave a half-written blob the next session reads back as garbage.
- `UserSettingsStorage.Delete` returns `bool` indicating whether the post-call state is "file does not exist", and routes any failure (locked file, missing permission) through `Debug.LogWarning` with the path. The four file-backed `Clear()` methods (`SnippetStore`, `RunHistoryStore`, `WatchStore`, `PatchPersistence` — and `PatchRegistry.Clear` which delegates to it) now propagate that flag, and `Tools / Roslyn REPL / Reset Project Data` aggregates the four results: any survivor surfaces a "partial failure" dialog listing the stuck files, the folder path, and the remediation (close any external editor, retry, or delete by hand) instead of the previous unconditional "Cleared N items" success. Without this the next `Load()` would silently resurrect the supposedly-cleared sensitive data and the user had no idea. PR-review followup.
- `SnippetStore.HasAny` / `RunHistoryStore.HasAny` / `WatchStore.HasAny` mirror the existing `PatchPersistence.HasAny` so Reset Project Data's scope decision counts file existence separately from successful Load. Without those, a corrupt or unreadable JSON would collapse to an empty list and trip the early-return "Nothing to clear" — leaving the actual sensitive file untouched on disk. The confirm dialog now phrases those rows as "the on-disk \<store\> file (currently unreadable / cannot be decoded — wiped to recover)" so the user sees what the wipe is actually targeting. PR-review followup.
- The Run History popup's Clear button now consumes the bool from `RunHistoryStore.Clear`. On failure the visible list refreshes from disk (so it matches the actual surviving file state instead of looking empty) and an inline error dialog points at the cause + the Console for the exact path. Previously a locked `runHistory.json` would leave the UI blank while the file — and its sensitive contents — sat untouched, ready to resurface the next time the popup opened. PR-review followup.
- File schema is JSON with a `version` field so future format bumps can be detected; payload shapes are `{version, items}` for the three list stores and a `SpecDto` mirror of `MethodPatchSpec` (with an explicit `HasOriginalBody` flag so `null` vs `""` source snapshots stay distinct across reload) for patches. README persistence + safety sections (EN + KR) updated to point at the new location and to call out the per-folder `.gitignore`. Closes #27.

### Fixed (Generic parameter types in patch specs)
- `MethodPatchSpec.ParameterTypes` now uses `;` as the per-parameter separator instead of `,`. Closed-generic CLR `Type.FullName` output embeds commas inside the assembly-qualified inner type list:

  ```text
  List`1[[System.Int32, mscorlib, Version=4.0.0.0, Culture=neutral, PublicKeyToken=…]]
  ```

  so the historic comma split in `PatchEngine.ResolveParamTypes` shredded any generic parameter into garbage and the spec failed to resolve at Apply / reload / source export — even though the form picker would happily list the method as patchable. Semicolon is illegal in CLR full type names, so it can't collide with anything inside a single parameter's name.
- New `MethodPatchSpec.JoinParamTypes` / `SplitParamTypes` helpers centralize the encoding. `Browse → Fill form` and the manual form field both go through the join helper; the engine resolver, the diff, and the active-list display all go through the split helper. Legacy comma-joined data still loads (split falls back to `,` when the value contains no `;` and no `[`), so plain non-generic specs persisted on 0.7.1 keep working. Legacy data that had embedded brackets (i.e. it was already broken) surfaces a clean "type not found" error from the type resolver instead of silently picking the wrong overload.
- New `MethodPatchSpec.NormalizeParamTypes(string)` runs at every registry boundary — `Keyed`, `AddOrUpdate`, and `LoadFromPersistence` — so a legacy comma-joined entry persisted on 0.7.1 lands at the same registry slot as a freshly Browsed semicolon-joined entry for the same method. Without it `Apply` couldn't see the previously-installed Harmony prefix and would stack a second one. Caught in PR review for #41.
- The Params field tooltip and the active-list row label now reflect the new semicolon-separated form. The row label still renders with `, ` between entries for readability — the raw semicolon form is only the on-disk encoding. Closes #41.

### Fixed (Generic-aware C# type renderer for patch wrappers)
- New `RoslynRepl.Editor.Patches.CSharpTypeName.Render(Type)` is the single renderer the patch wrapper uses for both the `Prefix` declaring-type / parameter-type emission and every `typeof()` reference inside generated helper bodies. Handles closed generics (`List<int>` / `Dictionary<string, int>`), nested types (the historic `+` → `.`), nested generics with per-level introduced-arity attribution (`Outer<int>.Inner` / `Outer<int>.Inner<string>`), arrays of any rank, and `Nullable<T>` (rendered as the `T?` short form). The historic `target.DeclaringType.FullName.Replace('+', '.')` only worked for plain non-nested non-generic types — anything with a backtick / bracket in the FullName produced CLR-internal arity markers in the wrapper source and the Roslyn compile failed even after the spec resolved correctly to a real method.
- `PatchSyntaxRewriter.TypeRef` and `PatchSyntaxRewriter.RenderCSharpTypeWithSubstitution` now route through `CSharpTypeName.Render` too. The rewriter previously carried its own copy of the renderer with the same backtick-truncation bug — so even after the wrapper signature was fixed, a body that read a `private Outer<int>.Inner<string>` member through `__get<T>` / `__call<T>` / `typeof(...)` would still emit invalid C# for that member's type and the wrapper would fail to compile on the next Apply. `CSharpTypeName.Render` gained an internal `(Type, Func<Type, string>)` overload so the rewriter's substitution callback (method-level generic argument → user `TypeSyntax`) can plug into the same nested-chain walk; the duplicate buggy renderer is gone. Reported in PR review.
- Open generic types (generic type definitions, generic parameters) raise `NotSupportedException` at the renderer; `PatchEngine.ResolveTargetMethod` catches the upstream form (`type.IsGenericTypeDefinition`) and rejects with a friendly "specify a closed type, e.g. `Foo<...>`" message before the wrapper renderer ever runs. Closes #42.

### Fixed (Watch fallback discovery cache)
- Watch evaluation no longer re-runs the global instance discovery sweep for every fallback expression in a Refresh. The earlier shape made each unqualified or owner-qualified fallback call `InstanceLocator.Find(InstanceCategory.All, …)` directly, and `Find` rebuilt the pool from scratch every time — `Resources.FindObjectsOfTypeAll<MonoBehaviour>()` + `Resources.FindObjectsOfTypeAll<ScriptableObject>()` + `SingletonScanner.Find()` for each call, twice per fallback expression (qualified pass + unqualified pass). N fallback watches paid the discovery cost 2N times in a single Run, which felt slow on projects with many loaded scenes.
- New `WatchEvaluationScope` opens once per `WatchEvaluator.RefreshAll` and lazily snapshots the global pool the first time any fallback expression needs it. Every later fallback call in the same refresh reuses the snapshot through `InstanceLocator.FindFromSnapshot`, which keeps the per-call filter + cap behaviour identical to the original `Find` path. The scope is disposed in a `finally` so destroyed Unity objects don't get pinned past the refresh — references are only held for the lifetime of one Refresh, never across refreshes. `EvaluateOne` called outside a Refresh (one-off API surface) opens its own throwaway scope so the standalone semantics survive. Closes #43.

## [0.7.1] - 2026-05-11

### Changed (OpenUPM package id)
- Package id renamed from `com.roslyn-repl` to `com.youngchan.roslyn-repl` so the package satisfies OpenUPM's reverse-domain package-name convention before the first public registry release.
- Installation snippets, OpenUPM metadata, the Harmony patch id, and package-root lookups now use the new id. Runtime/editor asset lookups resolve through Unity Package Manager metadata with a legacy `Packages/com.roslyn-repl` fallback so existing local checkout folders keep working during development.
- The OpenUPM descriptor was renamed to `Documentation~/openupm/com.youngchan.roslyn-repl.yml`, and the README now lists OpenUPM as the primary install path after the package went live at `https://openupm.com/packages/com.youngchan.roslyn-repl/`.

## [0.7.0] - 2026-05-11

### Fixed (Pull / Apply resolver parity)
- `PatchSourcePuller.TryPullMethodBody` and `PatchSourceWriter.ApplyToFile` now resolve the target method through the same `FindMethodForTarget` call. The earlier shape had Pull go through a private `FindMethodNode` with a richer fallback chain than the writer-side finder, so a method that pulled successfully could later fail to apply with "could not find a matching method" — leaving the user holding a body they couldn't write back. `FindMethodNode` is removed; `TryPullMethodBody` is now a thin wrapper that calls the shared finder, surfaces the expression-bodied error, and extracts the body via `ExtractBodyInside`.
- `FindMethodForTarget` runs as a pre-scan plus two passes:
  - **Pre-scan**: parse every candidate path once, collect all arity-matching `(TypeDecl, MethodDecl)` pairs, cache the per-file `SemanticModel` (or `null` if Roslyn's semantic build failed for that file).
  - **Pass 1 (strict)**: walk every cached context and return the first candidate whose semantic parameter types resolve exactly to the target's CLR types — across *all* paths, never stopping at a fallback in an earlier path. Without this, a partial class with per-file `using Model = …` aliases lets the first part's single-arity overload short-circuit the search and Pull / Apply commit to the wrong file even when a later partial holds the correct resolution.
  - **Pass 2 (fallback)**: only when Pass 1 fails. Collect a *global* fallback pool from every path, then auto-select only when the pool has exactly one member; multiple → return null so the caller surfaces a clear "Could not find a method matching ..." instead of guessing.
- Tri-state `ParamMatch` (`Exact` / `Unresolved` / `Mismatch`) replaces the earlier boolean matcher so Pass 2 can tell a known-wrong candidate from an uncertain one. Earlier the bool collapsed two cases — semantics couldn't resolve, vs semantics resolved and explicitly disagreed — and the latter was leaking into the fallback pool: a partial whose alias pointed at a *different* CLR type would still join the pool when other partials were missing or unparseable, and the global single-member auto-select would commit to that explicitly-wrong overload. Now Pass 2's semantic-built single-candidate branch only adds the candidate when the matcher returns `Unresolved`; `Mismatch` candidates are dropped.
- Pass 2 contributes per path:
  - Semantic-built file with `Unresolved` single candidate → joins the pool.
  - Semantic-built file with `Mismatch` single candidate → dropped (Roslyn already proved it wrong).
  - Semantic-built multi-candidate file → contributes nothing (Pass 1 already proved no strict match exists, and we won't guess between overloads).
  - Semantic-build-failed file with single arity candidate → joins the pool.
  - Semantic-build-failed file with multiple candidates → only the syntactic-name matches join.
- "First candidate as last-resort" is dropped on purpose. Silently committing to the wrong overload when nothing legitimately matched is exactly the failure mode this rewrite exists to fix; surfacing the failure gives the user the signal to qualify the spec instead of debugging a mysterious wrong body. Closes #30.

### Added (Run on Player Frame marshal)
- `RoslynRepl.Editor.Core.ReplEngine.ExecuteOnPlayerFrame(userCode, options, onComplete)` and `ReplCoroutineHost`: a hidden runtime MonoBehaviour the engine spins up lazily to host a coroutine that yields one frame before invoking the underlying `Execute`. The `Execute` call lands inside the next Player Update — the same phase a real `EventSystem`-driven `Button.onClick` would fire from — so popup spawns, canvas / SuperScrollView init, and any other logic that depends on the player layout cycle behave identically whether the snippet was launched from a real button or from the REPL Run path. Edit Mode (no Player Update to wait for) falls back to a synchronous `Execute` so the helper is still safe to call when the editor isn't playing. Result is delivered through `Action<ReplResult>` so the call site doesn't have to spin-wait the main thread.
- `RoslynReplWindow.RunOnPlayerFrameEnabled` machine-wide pref (default `true`) + `Tools / Roslyn REPL / Run on Player Frame` checkable menu item with the same validate-pass refresh pattern the auto-reapply toggle uses. The window's `Run` handler routes through `ExecuteOnPlayerFrame` only when the pref is on AND the editor is in Play Mode; otherwise it stays synchronous so Edit Mode inspections / Watch refresh / unit-test paths feel unchanged. Diagnoses (issue tracked separately) where the same one-line `ShowPopupAssetBundle` call gave different results from a real button vs. the REPL — frame phase was the underlying difference.
- `ReplCoroutineHost` lives in the same .cs as `ReplEngine` on purpose: a freshly added Editor-folder MonoBehaviour can occasionally fail to compile on the same domain reload that introduces it, leaving the engine with an unresolved reference. Same-file sibling makes the dependency atomic.

### Fixed (Apply-to-file snapshot guard at click time)
- `MethodPatchView.OnApplyToFileClicked` now resolves the source snapshot up front (right after target resolution, before the confirm dialog) and aborts the click with a status hint when no snapshot exists for the current form's target. The `Apply to file` button's enable state lives on `RefreshDiff`, which the diff debounce delays by `DiffDebounceMs` after the user's last keystroke — so a Type / Method / Params edit could leave the button enabled against a stale (Pull-Original-for-A) state for up to that window. A click inside the window used to flow `snapshot = ResolveSnapshot()` (now keyed on target B → null) → `PatchSourceWriter.ApplyToFile(target, body, null)`, and the writer's conflict check is gated on `expectedSnapshot != null`, so a null snapshot meant a no-comparison overwrite of the new target's source file. Click-time guard re-runs `ResolveSnapshot` against the current form key, surfaces a "Pull Original first" message when null, and calls `RefreshDiff` so the diff section + button enable state catch up immediately instead of waiting out the debounce tail. The dialog is modal, so reusing the captured snapshot for the actual `ApplyToFile` call after Apply is safe (no other ResolveSnapshot needed). Reported in PR review for #26.
- `MethodPatchView` now routes the four `RegisterValueChangedCallback` hooks (Type, Method, Params, body) through a single 200 ms scheduled `RefreshDiff` instead of running the diff synchronously on every keystroke. `PatchSourceDiff` is an O(N*M) line-LCS, so a freshly Pull'd long method that the user is editing was firing a fresh full-body diff per keystroke and showing up as input lag in UI Toolkit. The schedule handle is paused + reassigned per change so the eventual diff runs against the final form state, not a stale snapshot from the start of a typing burst, and `Dispose` pauses the pending fire so a teardown can't surface a `RefreshDiff` against torn-down fields. Direct `RefreshDiff()` calls from non-keystroke paths (initial render, `LoadIntoForm`, `FillFormFromMethod`) keep their immediate semantics — those are explicit user actions, not typing bursts. Same 200 ms budget as the Object Browser's search debounce so the two debounced surfaces feel consistent. Closes #26.

### Fixed (Object Browser bounded ordering — stable tie-break)
- `InstanceLocator.FindBounded` now wraps each candidate in a `RankedEntry` carrying the candidate's filter-pass enumeration ordinal, and the bounded comparer uses that ordinal as a fourth key after `IsActive desc / TypeName / DisplayName`. The earlier shape relied on `List.BinarySearch` for the sorted-prefix insert, but `BinarySearch` doesn't promise a deterministic index for entries the comparer reports as equal — meaning a later same-key row could splice in front of an earlier one, and the bounded preview would diverge from `unbounded[0..max]` at duplicate positions. LINQ's `OrderBy / ThenBy` chain (still used by `FindUnbounded`) is a stable sort by spec, so making `FindBounded`'s comparer total-ordered with the ordinal key restores the byte-identical-prefix promise. Reported in PR review for #25.
- Verified via Roslyn probe in the live project: 7 distinct duplicate-key groups exist in the All pool (largest: `InnerResolver / InnerResolver.Instance / Active=True × 12`), and `bounded[0..99]` matches `unbounded[0..99]` by `Value` reference identity for every position.

### Fixed (Object Browser scan cap — review notes)
- `InstanceLocator.Find` no longer materializes + sorts the full pool when the caller passes a finite `maxResults`. The previous shape ran `OrderByDescending(IsActive).ThenBy(TypeName).ThenBy(DisplayName).Take(maxResults).ToList()`, which forced a full O(N log N) sort over every candidate before the cap took effect. Cap-200 was a UI-row limit, not a scan-cost limit. Reported in PR review for #25.
- New private `FindBounded` path: walks every candidate once, but maintains only a sorted prefix of size `maxResults` via `BinarySearch`-ed insert + tail truncate. The shared `_orderComparer` mirrors the original LINQ chain (Active descending → TypeName ordinal → DisplayName ordinal). An early-skip — "if the prefix is full and this entry would land past index `maxResults`, drop it without inserting" — lets the common case (long pool, small cap) avoid the insert + shift cost entirely.
- Result is byte-identical to the unbounded prefix, so the bounded preview the user sees on every Refresh matches what "Load more" would surface; no cross-category bias from an earlier draft that broke the enumerator early. Verified via probe (50/50 entries match the unbounded `[0..49]`).
- `FindUnbounded` keeps the original LINQ shape verbatim — `Load more` callers ask for `int.MaxValue` and get the full sorted list back.

### Added (Object Browser scan cap + debounce)
- `ObjectBrowserView` now caps the per-Refresh result count at `DefaultMaxResults = 200` instead of `int.MaxValue`. The previous shape ran an unbounded `InstanceLocator.Find` on every category change, every keystroke, and every refresh-button click — a fresh sweep + sort + ListView rebuild on the editor main thread that could stall for seconds in projects with thousands of MonoBehaviours / ScriptableObjects. The cap keeps the common case responsive without dropping access to the full list.
- Search field RegisterValueChangedCallback now schedules `Refresh` via `_root.schedule.Execute(...).StartingIn(200ms)` instead of firing on every keystroke. The handle is paused + reassigned per keystroke so the eventual scan runs against the final text. Category dropdown bypasses the debounce — that change is one explicit click, not a typing burst.
- Status row gains a "Load more" button that surfaces only when `Refresh` returns at least `DefaultMaxResults` entries (treated as "may have hit the cap"). Click flips a `_showAll` flag and re-runs the scan unbounded so the user can opt into the long path on demand. Status text branches: `"N item(s)"` (under cap), `"N+ items — capped at 200, click Load more for the full scan"` (cap hit), `"N item(s) (showing all)"` (post-Load-more). `_showAll` resets on category / search change so a heavy unbounded sweep on filter A doesn't bleed into filter B. Closes #25.

### Fixed (Dynamic-assembly counter — review notes)
- `RoslynRepl.Editor.Diagnostics.ReplDiagnostics.DynamicAssemblyCount` now also recognizes the runtime-patch path's `ReplPatch_*` prefix in addition to the REPL snippet path's `ReplDynamic_*`. The previous counter only matched the snippet prefix, so a session of repeated Apply Patch / re-Apply churn would build up unload-blocked assemblies that the toolbar badge / Verify Setup line / Force Domain Reload dialog all showed as zero — exactly the visibility gap the diagnostic was meant to close. New `ReplDiagnostics.IsReplGeneratedAssembly(name)` predicate is the canonical check; `ReplEngine.OnAssemblyLoaded`'s self-skip and `SetupVerifier`'s count both route through it so a future prefix rename only edits one spot. Reported in PR review for #24.
- `ReplDiagnostics.AssemblyCountChanged` is now coalesced and raised on the editor main thread. The previous shape called subscribers directly from the loader thread, which UI Toolkit can't safely service — a Mono background loader would race the editor frame and the badge repaint would either drop silently inside the existing try/catch or land outside the main-thread invariant tools assume. New shape: `OnAssemblyLoad` does only an `Interlocked.Increment` on a pending counter; an `EditorApplication.update` tick atomically clears the counter and invokes subscribers on the main thread. Bursty loads (Apply Patch + its rewrite passes + dependent assemblies) collapse into one repaint per frame, which is also the cheapest thing to do. Reported in PR review for #24.

### Added (Dynamic-assembly accumulation diagnostics)
- `RoslynRepl.Editor.Diagnostics.ReplDiagnostics`: new `[InitializeOnLoad]` static class exposing `DynamicAssemblyCount` (live count of `ReplDynamic_*` assemblies the engine has loaded since the last domain reload), `AssemblyCountChanged` event (fires on every `AppDomain.AssemblyLoad` so subscribers repaint without polling), and `WarnThreshold` / `HighThreshold` / `SeverityOf(int)` so UI surfaces switch on a single source of truth instead of duplicating threshold checks.
- `Tools / Roslyn REPL / Force Domain Reload`: confirm-dialog menu item that calls `EditorUtility.RequestScriptReload()`. Dialog reports the current dynamic-assembly count and the side effects (in-memory `_` reset, every `[InitializeOnLoad]` static ctor re-runs) so the user can decide before clicking Reload.
- REPL window toolbar gains a `💾 N asm` pill between the patches badge and the version label. Hidden when zero; muted grey under `WarnThreshold`; yellow + bold at or above `WarnThreshold`; red + bold at or above `HighThreshold`. Click routes to the Force Domain Reload confirm dialog. Subscribes to `ReplDiagnostics.AssemblyCountChanged` and unsubscribes in `OnDisable`.
- `SetupVerifier` output upgrades the dynamic-assembly line tag (`[diag]` → `[hint]` → `[warn]`) based on the same severity, and appends a "consider Force Domain Reload" line when the count crosses either threshold so users running Verify Setup see the same advice the badge surfaces. Closes #24.

### Fixed (Dormant copy aligned with immediate-install)
- Updated the strings that contradicted the new toggle behavior: the active-list dormant row tooltip in `MethodPatchView` no longer says "toggle the Tools menu setting back on for the next reload" (which was true before the immediate-install setter and stale after); the inline toggle tooltip now spells out "Flipping this back on installs every dormant patch immediately, no reload needed"; the Bootstrap auto-off log line points users at the inline toggle and the same install-now phrasing; the `PatchAutoReapply` class XML doc and the Bootstrap inline four-scenario table call out the OFF→ON edge case explicitly. README's runtime-method-patching paragraph picks up a single trailing clause covering the "turn it back on to install everything" path. `PatchRegistry.NotifyInMemoryMutation`'s XML doc also reframes around the dormancy-set design (it had carried over a "mark Inactive in memory" line from the pre-session-set draft). Reported in PR review for #22.

### Fixed (Toggle ON honors the immediate-install promise)
- `PatchAutoReapply.AutoReapplyEnabled` setter now installs every dormant spec immediately on an OFF→ON transition, instead of waiting for the next domain reload. The earlier shape only flipped the pref + raised `SettingsChanged`, which contradicted the toggle tooltip and the Patches-tab inline copy ("toggle this back on … to install now"): rows kept rendering as `(auto-off)` and stayed uninstalled until reload or per-row Apply. New helper `ReapplyDormantSpecs` walks specs whose `Status == Active` and `IsSessionDormant`, calls `PatchEngine.Apply` on each, and records `Failed` for any that throw — same handling as the Bootstrap auto-on path. `Apply`'s `AddOrUpdate` clears the dormancy mark as part of its normal write, so a successful re-install ends with the row at `DisplayState.Active`. Verified via Roslyn probe (3/3): pre-toggle two `DormantAutoOff` specs → flip ON → both dormancy marks cleared and neither row stays in `DormantAutoOff`. Reported in PR review for #22.

### Fixed (Revert All scope + auto-reapply toggle placement)
- `RoslynRepl.Editor.Patches.PatchEngine.RevertAll` now iterates the registry instead of `_applied`. The earlier shape walked `_applied.Keys` only, which silently skipped any spec the user had disarmed via auto-off — those specs keep `MethodPatchSpec.Status = Active` for persistence reasons but have no detour installed, so they were absent from `_applied` and Revert All reported "0 reverted" while leaving them persisted as Active. Flipping auto-reapply back on (or opening the project on a workstation with the default setting) would re-install every "supposedly reverted" patch on the next reload. Now the loop selects every spec whose `PatchRegistry.GetDisplayState` resolves to `Active` or `DormantAutoOff` and calls `Revert(spec)`, which flips Status to Inactive and persists. A second pass over `_applied` flushes any orphaned detours whose specs were already removed from the registry. Verified via Roslyn probe (4/4): two dormant specs revert as expected, the dormancy mark gets cleared, and a simulated post-revert reload installs zero patches. Reported in PR review for #22.
- Auto-reapply toggle now appears inline at the top of the Patches view as well as the Tools menu. Same setting (`PatchAutoReapply.AutoReapplyEnabled`); the `SettingsChanged` event keeps both surfaces in sync regardless of which one the user clicks. Tooltip explains the on/off semantics so users can flip it without leaving the panel.

### Fixed (Runtime patch display state — review notes)
- `RoslynRepl.Editor.Patches.PatchDisplayState`: new four-value enum (`Inactive` / `Active` / `DormantAutoOff` / `Failed`) that owns "what should the UI render for this spec right now". Centralizes the rule that auto-off-dormant specs keep `MethodPatchSpec.Status = Active` for persistence reasons but must render as a distinct state because no Harmony detour is installed.
- `RoslynRepl.Editor.Patches.PatchRegistry.GetDisplayState(MethodPatchSpec)`: single source of truth that folds the persisted `Status`, the live `PatchEngine.IsApplied` map, and the session dormancy set into one enum. UI surfaces — toolbar badge in `RoslynReplWindow.UpdatePatchBadge`, active-list row in `MethodPatchView.RebuildActiveList`, form status line in `UpdateStatusForCurrentForm`, and the Apply-failure preservation gate in `OnApplyClicked`'s catch branch — now switch on the enum instead of composing `spec.Status == Active && IsSessionDormant && IsApplied` themselves. Adding a new UI surface that needs to know the state can no longer accidentally drop a clause.
- Two specific bugs the central decision fixed (PR review notes for #22 round 4):
  - **Loaded dormant rows said "Active on …" in the form status.** `UpdateStatusForCurrentForm` only switched on `spec.Status` so a `Load`'d dormant spec read as Active even though no detour was installed. The new branch on `PatchDisplayState.DormantAutoOff` shows `"Target.Method (auto-off — Apply to install now)"` instead.
  - **Apply-failure catch path treated dormant specs as a live previous patch.** When the new body failed to compile, `existing.Status == PatchStatus.Active` was the gate that suppressed the Failed registry update — but a dormant spec is Active on disk with no detour, so the gate fired falsely and the user lost the diagnostic for that row. Routing through `GetDisplayState(existing) == PatchDisplayState.Active` only preserves when a detour is actually installed, so dormant rows now record the failed attempt and surface the compile error instead of being suppressed.
- Verified via Roslyn probe (5/5 enum cases + toolbar count match + catch-branch gate). The probe synthesizes specs without resolving real targets — `IsApplied` returns false for all of them — confirming that "Active on disk + no live detour" lands in `DormantAutoOff` even without going through the auto-reapply opt-out flow.

### Fixed (Runtime patch reapply — review notes)
- `PatchAutoReapply.Bootstrap` no longer mutates `MethodPatchSpec.Status` for the auto-off opt-out. The earlier two attempts kept landing on the same shape: even when persistence was skipped at the bootstrap site, the in-place Status mutation on the same instance held inside `PatchRegistry._byKey` survived until the next unrelated registry write — Apply on a different spec, adding a draft, removing a row, anything that called `Persist()` over `_byKey.Values` — at which point the in-memory Inactive serialized to EditorPrefs and the persisted reapply intent was lost indirectly. Reported in PR review for #22.
- New separation: `RoslynRepl.Editor.Patches.PatchRegistry` ships a session-only `_sessionDormantKeys` set (`HashSet<string>` keyed on `MethodPatchSpec.Key`) and three public helpers — `IsSessionDormant`, `MarkSessionDormant`, `ClearSessionDormancy`. The auto-off branch records dormancy in this set and never touches `spec.Status`. `AddOrUpdate` and `Remove` automatically drop the corresponding key from the set when the user takes an explicit action on that spec (Apply, Revert, draft save, deletion); `Clear` wipes the whole set as part of the project reset. UI surfaces (toolbar badge in `RoslynReplWindow.UpdatePatchBadge`, active-list rendering in `MethodPatchView`) consult `IsSessionDormant` alongside `spec.Status`, so an Active+dormant spec renders with the inactive-color dot and an `(auto-off)` suffix and contributes to the dormant count rather than the active count.
- Verified via Roslyn probe (3/3): the original "no-leak" scenario — auto-off mark on spec A, then `AddOrUpdate` on an unrelated spec B forcing `Persist` over `_byKey.Values`, then `PatchPersistence.Load()` reads spec A's Status — now reads Active. Explicit `AddOrUpdate` on a dormant spec clears the mark. Simulated reload (`LoadFromPersistence`) re-hydrates the spec as Active, ready for the auto-on reapply path.

### Added (Runtime patch reapply visibility)
- `RoslynRepl.Editor.Patches.PatchAutoReapply.AutoReapplyEnabled`: machine-wide opt-out toggle (default `true` — preserves historical behavior). When off, the bootstrap path still hydrates the persisted registry but demotes every `Active` spec to `Inactive` on reload and writes `LastError = "Auto-reapply is disabled — Apply manually to re-install."`. The user must explicitly Apply each row from the Patches view (or flip the menu back on) to re-install Harmony detours, which is the "explicit user confirmation" half of the issue's acceptance criterion.
- `Tools / Roslyn REPL / Auto-reapply Patches on Reload`: checkable Tools menu item with a `[MenuItem]` validator that re-syncs the checkmark from `EditorPrefs` on every menu open so a flip from elsewhere shows correctly. `PatchAutoReapply.SettingsChanged` event lets UI surfaces (the toolbar badge) react to the toggle without polling.
- REPL window toolbar gains a `🔧 N active` orange pill that surfaces the count of currently-installed runtime patches between the mode badge and the version label. Hidden when no patches are active so the toolbar stays calm during normal use; rendered dimmer (`🔧 N (auto-off)`) when stored patches are not currently installed because auto-reapply is disabled, so a user spotting the pill never confuses an empty registry with a registry full of dormant specs. Click routes to the Patches view. Subscribes to `PatchRegistry.Changed` + `PatchAutoReapply.SettingsChanged` and unsubscribes in `OnDisable`.
- Auto-reapply summary console line now ends with `(Toggle '<menu path>' to disable.)` so a user noticing the redirect re-installation has a one-jump path to opt out. Closes #22.

### Fixed (Source export — atomic write)
- `RoslynRepl.Editor.Patches.PatchSourceWriter.ApplyToFile` now uses `File.Replace` instead of `File.WriteAllText` over the original file. The previous shape was `File.Copy(...bak)` followed by a direct `File.WriteAllText(...source)`, which left a window where a half-written `.cs` could land on disk if the write failed mid-stream (out-of-disk, permissions flip, host crash). The new shape is three-step: pre-create the `.bak` via `File.Copy`, write the new contents to a dot-prefixed temp file in the same directory (so `File.Replace` stays on the same volume and remains OS-atomic), then `File.Replace(temp → source, .bak ← previous source)` to swap. A failure during the temp write or the replace leaves the original file untouched and the pre-write `.bak` valid for recovery; the temp file is best-effort cleaned up. Verified against simulated failures: temp write to a non-existent volume leaves the source byte-identical; `File.Replace` against a missing temp source raises and leaves both source + backup intact. Closes #21.

### Added (Cooperative-cancel warning)
- First-Run safety dialog (`Tools / Roslyn REPL / Open` → Run): one-time confirm on each workstation explaining that snippets execute synchronously on the Editor's main thread and that the timeout / external Cancel only fire when the snippet observes `ct`. `while (true) {}` and other non-cooperative loops will freeze the Editor; the dialog wording surfaces that explicitly so a first-time user can't trip the freeze without ever having read the safety section. Cancel aborts that single Run only — no ack, no Run. Ack is stored in machine-wide `EditorPrefs` (`RoslynRepl.CoopCancelAcknowledged`); a fresh workstation re-prompts.
- Persistent yellow banner under the Code header repeats the warning after ack: "Snippets run on the Editor main thread. `while(true)` without `ct` checks freezes Unity — only cooperative cancel works." Same text reused as the Run button's tooltip, so hover-discoverable surfaces never disappear once the dialog is dismissed.
- README **Safety Notes** gains a **Cooperative Cancel Only** subsection explicitly stating no hard-kill exists for non-cooperative snippets, with a `ct.ThrowIfCancellationRequested()` example and a note that `Thread.Abort` / isolated worker domains are unavailable on Unity's runtime. Pairs with the existing Troubleshooting "The Editor Becomes Unresponsive" entry. Closes #20.

### Fixed (Watch compile cache — review notes)
- `RoslynReplWindow.ResetProjectData` now invalidates `ReplEngine`'s compile cache and includes `ReplEngine.CompileCacheCount` in the reset scope check + dialog counts. The previous flow cleared `WatchStore` (which holds the user's expression list) but left the cache dictionary intact — and the dictionary keys are the full wrapped source (usings + user expression text) plus a strongly referenced `Assembly` and `MethodInfo`. After running sensitive watches, Reset would tell the user "watches cleared" while that exact text stayed alive in process memory until the next `AppDomain.AssemblyLoad`. Reported in PR review for #13. The early-return "nothing to clear" branch now also checks `compileCacheCount == 0`, the confirm dialog lists cached compiled watches as a separate row, and the success dialog's tally includes them in the cleared total.

### Added (Watch compile cache)
- `RoslynRepl.Editor.Core.ReplOptions.UseCompileCache`: opt-in flag that lets `ReplEngine.Execute` reuse a previously compiled dynamic assembly + entry-point `MethodInfo` when the wrapped source — usings, class shell, user expression — matches an earlier call byte-for-byte. The Watch panel turns it on so a refresh of N rows over many user Runs amortizes to one compile per row instead of N×Run compiles. Off by default for the main editor: each Run stays a fresh compile against the latest editor state.
- Cache keyed on `WrappedCode.Source` (full wrapped source including effective usings list). Different snippet *or* different usings ⇒ different key ⇒ fresh compile by construction, so a hit never rebinds against stale namespace state.
- `AppDomain.AssemblyLoad` invalidates the entire cache for any non-`ReplDynamic_*` assembly load — package install, user-script recompile, satellite assembly resolution. Engine-emitted dynamic assemblies are skipped (each cache miss ends with `Assembly.Load`, and self-wiping every miss would defeat the feature). Verified: adding a package mid-session forces the next Watch refresh through a fresh compile.
- Diagnostic surface: `ReplEngine.CompileCacheCount` returns the current entry count, `ReplEngine.InvalidateCompileCache()` exposes the clear hook for tests + a future "force rebuild" UI button. Closes #13.

### Added (Source export — diff + Apply to file)
- `RoslynRepl.Editor.Patches.PatchSourceDiff`: line-level LCS diff between the source snapshot Pull Original captured (or `spec.OriginalBody` for a stored patch) and the current edited body. Returns `Same` / `Added` / `Removed` line tags and a `FormatUnified` helper that emits a `--- / +++ / [space|+|-]`-prefixed text blob suitable for clipboard paste.
- `RoslynRepl.Editor.Patches.PatchSourceWriter.ApplyToFile`: locates the declaring type's source file via the same MonoScript walk Pull Original uses, splices between the target method's outer braces with the user-edited body. The auto-rewrite that routes inaccessible names through reflection helpers is wrapper-only and never touches `spec.PatchBody`, so the body written stays clean source — natural `hp -= 10`, never the wrapper-only `__set("hp", __get<int>("hp") - 10)`. A `.bak` sibling of the target file is saved before each write so failed runs leave the original intact, and `AssetDatabase.ImportAsset` is invoked so the Editor picks up the change without manual focus / re-tab. Expression-bodied methods are explicitly rejected with a "convert to `{ … }` first" reason — the splice anchors at the open / close braces.
- `MethodPatchView` Patches view gains a Diff section between the Apply / Revert action row and the Active patches list. Live-updated through `RegisterValueChangedCallback` on the Type / Method / Params / body fields. Same / Added / Removed lines are colored gray / green / red. Two buttons:
  - **Copy diff** — writes a unified-diff text blob to the system clipboard. Status line confirms the line counts that were copied.
  - **Apply to file** — confirmation dialog → `PatchSourceWriter.ApplyToFile` → status line surfaces the rewritten path + the `.bak` backup path on success, or the writer's specific failure reason on failure. Routes target lookup through `PatchEngine.TryResolveTargetMethod` so the same scope rules (instance void only, no generics / refs, etc.) apply that Apply Patch would enforce.
- Browse Methods + Object Browser × Patches double-click now auto-trigger Pull Original immediately after filling Type / Method / Params, so the body editor is ready to edit instead of empty after a single click. The Pull path's "non-default body — overwrite?" confirm dialog still protects existing edits.

### Added (Natural-code patch bodies)
- `RoslynRepl.Editor.Patches.PatchSyntaxRewriter`: diagnostic-driven Roslyn pass that takes the wrapper source `PatchCodeGenerator` produces, asks Roslyn to compile it, and rewrites every "name doesn't exist" / "inaccessible due to protection level" / "no definition" location into a reflection helper call. The user can write `hp -= 10`, `Singleton.Instance.privateField`, `MyClass.PrivateStaticMethod()` and have the patch compile without spelling `__get<int>("hp") - 10` etc. by hand. Code that compiles untouched (public access, locals, `transform.position`, `Mathf.Min(...)`, etc.) is left exactly as the user wrote it, so direct calls keep their fast codegen — the rewriter only kicks in for names Roslyn already proved direct access doesn't work for.
- Six new reflection helpers in the wrapper (the helper extension, alongside the original `__get` / `__set` / `__call`): `__getOn<T>(target, name)` / `__setOn(target, name, value)` / `__callOn<T>(target, name, args)` for arbitrary instance objects, `__getStatic<T>(type, name)` / `__setStatic(type, name, value)` / `__callStatic<T>(type, name, args)` for static reflection on an arbitrary `Type`. The shared `__InvokeReflective` pipeline picks an overload by argument count first, then narrows by runtime argument types when more than one candidate remains, falling back to the first arity-matching overload — better than throwing `AmbiguousMatchException` at the user.
- `PatchEngine.CompileToAssembly` now takes `(source, declaringType, specKey)` and runs the rewriter pass between Roslyn parse and Emit. Iteration is mandatory, not paranoid: when an outer write like `hidden = hp + armor` is replaced first, the inner `hp`/`armor` reads live in the new helper expression and need a second compile-then-rewrite pass to be re-flagged. The loop converges on the fix point, capped at 8 passes. The first-pass error guard keeps the runtime cheap — patches already in helper form (the documented style) compile clean on the first try and skip the rewriter entirely.
- Per-Apply summary line on the Editor console reports the rewrite count and pass count when the rewriter fires, e.g. `[Roslyn REPL] Patch 'Hero::Punch::System.Int32' rewrote 5 access(es) in 2 pass(es): hp (-=) → __set/__get; …`. Quiet when nothing was rewritten; useful as a debug breadcrumb when a rewrite goes subtly wrong.
- Diagnostic IDs the rewriter knows how to repair: `CS0103` (unqualified name not in scope), `CS0117` (static "no definition for member"), `CS0122` ("inaccessible due to protection level"), `CS1061` (instance "no definition for member, no extension method"). Anything else stays in `Result.Unhandled` and surfaces as a normal compile error.

### Limitations (intentional)
- **`async` / `await` not rewritten.** Async patch bodies are out of the rewriter's scope. The rewriter visits expressions and assignments synchronously; an `await x.PrivateAsyncMethod()` inside a void method body would mix the rewrite with state-machine codegen the helpers don't speak.
- **Expression-context postfix `++` / `--` is exact for statements only.** `hp++;` as a statement rewrites cleanly to `__set("hp", __get<int>("hp") + 1)`. `var x = hp++;` (read the old value, then increment) would need a temporary variable to preserve postfix semantics; the rewrite emits the prefix-equivalent and the user receives the new value, which is wrong for that one expression-context pattern. Document, don't auto-correct.
- **Generic method type inference is not auto-rewritten, but explicit-argument generic calls work.** `Cache.Instance.GetPrivate<Foo>(...)` and `MyType.MakeFor<Bar>(...)` route through new `__callG` / `__callGOn` / `__callGStatic` helpers, which call `MakeGenericMethod(...)` before invoke and substitute the user-supplied type argument into the helper's `<R>` when the method's return type is a generic parameter (the canonical `T Foo<T>()` shape). Type-inferred calls — `Cache.Instance.GetPrivate(...)` letting the compiler pick T — still need explicit `<...>` because the helpers don't reproduce the inference path.
- **`ref` / `out` / `in` parameters stay rejected.** the engine blocks them at `ResolveTargetMethod`, and the helpers can't synthesize a ref-aware reflection call. Documented limitation.
- **No write to `readonly` / get-only properties.** Reflection's `SetValue` would throw at runtime; the rewriter doesn't pre-check, so the user sees the exception when the patch runs. This matches the direct-access behavior — `readonly` is enforced by the runtime, not the rewriter.

### Added (Pull Original)
- `RoslynRepl.Editor.Patches.PatchSourcePuller`: locates a target method's `.cs` file via `MonoScript.GetClass()` matching, parses the file with Roslyn, finds the method by identifier + parameter count (with type-name disambiguation for overloads), and extracts the text between the outer braces. Returns a structured `PullResult` with `Success` / `Body` / `SourcePath` / `Error` so the UI can either populate the editor or show a specific failure message ("No MonoScript found", "expression-bodied — Pull doesn't unwrap those", "arity mismatch", …).
- `PatchEngine.TryResolveTargetMethod`: public non-throwing wrapper around the existing `ResolveTargetMethod` so the Pull UI can reuse the same the supported scope checks (void instance only) without catching exceptions.
- `MethodPatchView` Patch body header gains a "Pull Original" button. Clicking pulls the target method's source body into the editor, preserving original indentation. Non-default existing bodies prompt for overwrite via `EditorUtility.DisplayDialog`. Status line reports the source path + char count on success, or the specific Error from `PatchSourcePuller` on failure.
- `RoslynRepl.Editor.UI.MethodPickerPopup`: utility window that lists every patchable method on a given Type — the supported scope filtered (void instance, non-generic, no ref/out, not a getter/setter accessor), with a search box and per-row visibility + declaring-type sub-line. Shared by two entry points so the picker UI lives in one place.
- `MethodPatchView` Patches form: Type / Method / Params collapsed into a single horizontal row, with a new `Browse` button that opens `MethodPickerPopup` on the current Type. Picking a method fills `Method` + `Params` (comma-joined `ParameterType.FullName` list) via a new public `FillFormFromMethod` helper. Saves users from typing exact method signatures.
- Object Browser × Patches integration: in Patches mode, double-clicking a Browser row opens `MethodPickerPopup` on that instance's runtime type (or the entry's `DeclaredType` when no live instance is available — common for singleton entries) instead of inspecting the instance into Output. The picker's callback routes through the same `FillFormFromMethod` path so click → click → patch form is fully populated.
- Patches view layout overhaul (PR feedback "Patches 길이나 스크롤 등 불편한점이 한두개가 아니야"): outer wrap removed, body editor sits inside its own vertical `ScrollView` so long bodies scroll inside the editor while form / actions / list stay anchored, active patches list pinned to its own bounded `ScrollView` (min 80 / max 160) so a long list doesn't push the body offscreen, body field minHeight bumped 110 → 200.

### Fixed (Pull Original — review notes)
- `MethodPatchView` now ships the source body Pull Original retrieved into `spec.OriginalBody` on Apply, so the snapshot source-export's diff view will rely on actually persists. The previous flow only filled the body editor; on Apply the spec was created with `OriginalBody = ""`, so after edit + Apply + reload the registry had no record of what the original method looked like. The view now caches the most recent successful Pull keyed by the form's identity (`{Type}::{Method}::{Params}`), invalidates that cache when the form changes (Type / Method / Params edited, Browse-picked a different method, Load loaded a stored spec without an OriginalBody), and copies the snapshot into the spec only when the keys still match — never persisting a stale body against the wrong target. Reported in PR review for #14 phase-c.
- `PatchSourcePuller.FindMethodNode` now scopes its candidate set to the declaring type's syntax declarations rather than every `MethodDeclarationSyntax` in the file. The previous walk was `root.DescendantNodes().OfType<MethodDeclarationSyntax>()` filtered only by name + arity, so a file declaring two top-level classes (or an outer class plus a nested helper) that happened to share a method name + parameter count could return the wrong body — and the disambiguation step's `return candidates[0]` fallback meant the wrong one would silently win when type names didn't disambiguate (e.g., both are zero-arg). The new path resolves every `TypeDeclarationSyntax` whose simple name + ancestor chain matches the target type (covers nested types and partial classes within one file), then enumerates only those declarations' direct method members. Reported in PR review for #14 phase-c. Verified via probe: in a file containing `class Outer { class Decoy { void Touch(); } void Touch(); }` (decoy declared first in source order), pulling `Outer.Touch` now returns the outer body where the previous code returned the nested decoy's.
- `PatchSourcePuller.AncestorChainMatches` now compares the enclosing namespace chain too. The previous version only walked syntactic *type* ancestors and broke at the first namespace, so a single `.cs` declaring `namespace A { class Player { void Hit(); } }` and `namespace B { class Player { void Hit(); } }` saw both Player declarations pass the name + (empty) ancestor type chain check; whichever appeared first in source order silently won, and Apply could end up patching `B.Player` while the body shown came from `A.Player`. The walker now collects every enclosing `NamespaceDeclarationSyntax` (and `FileScopedNamespaceDeclarationSyntax`, accessed via reflection so older Microsoft.CodeAnalysis builds without that type still compile), joins them outer-first with `.`, and compares the result against `declaringType.Namespace ?? ""`. Reported in PR review for #14 phase-c. Verified via probe: in a file containing both `RoslynReplTest.AaaPlayerHost.NamespaceProbe.Touch` (logs "AAA") and `RoslynReplTest.BbbPlayerHost.NamespaceProbe.Touch` (logs "BBB"), pulling each now returns its own body instead of letting source order decide.

### Limitations (intentional)
- Void instance methods only — same the supported scope. Static, non-void, ref/out, generic, constructor, property accessor are all rejected before Pull even runs.
- Block bodies only (`{ … }`). Expression-bodied methods (`=> expr`) are detected and surfaced as a specific error rather than silently returning an empty body.
- Source must live in `Assets/` or `Packages/`. Methods declared in precompiled DLLs (an SDK package without source, Unity-shipped types, etc.) have no MonoScript path to resolve and surface "No MonoScript found".
- Partial classes work (multiple paths walked in order until a matching method node resolves), but auto-generated partials (Unity codegen, source generators) may carry method bodies the user can't easily edit anyway.
- ~~the rewriter will add a Roslyn syntax rewriter so the user can write `hp -= amount;` instead of `__set("hp", __get<int>("hp") - amount);` against a Pull'd body.~~ **Resolved in the rewriter below.** Pull'd bodies now compile as-is; the rewriter routes inaccessible names through the helpers automatically.

### Added (Runtime patch persistence + domain-reload reapply)
- `RoslynRepl.Editor.Patches.PatchPersistence`: project-scoped EditorPrefs store for the runtime patch list. Each spec is six base64-encoded fields (target / method / params / original body / patch body / status) joined by `|`, specs joined by `\n`. Single malformed entry skipped, rest of the set still loads. `LastError` deliberately not persisted — it's transient diagnostic state from the last apply attempt.
- `PatchRegistry.AddOrUpdate` / `Remove` / `Clear` now write through to `PatchPersistence` automatically; the in-memory dictionary stays the read path. `PatchRegistry.LoadFromPersistence()` is the inbound counterpart used by the boot path. Authoring-side code (UI, engine) is unchanged.
- `RoslynRepl.Editor.Patches.PatchAutoReapply` (`[InitializeOnLoad]`): on every domain reload, deferred one Editor frame, loads the persisted spec list and re-installs each `Active` patch through `PatchEngine.Apply`. Successful re-applies stay Active; failures flip to `Failed` with `LastError = "Auto-reapply failed: …"` and persist the new status so the next boot doesn't repeat the noise. Inactive / Failed specs are loaded so the UI remembers the user's drafts but are not retried.
- `MethodPatchView`: failed rows now show their `LastError`'s first line as a persistent italic secondary label under the row (`↳ Auto-reapply failed: …`). Hovering still surfaces the full message; the inline preview is one line only so multi-line compile diagnostics don't shift the row layout.

### Added (Runtime Method Patch)
- `RoslynRepl.Editor.Patches.MethodPatchSpec`: identifies a patch by `(TargetTypeName, MethodName, ParameterTypes)` triple plus the patch body, original-body snapshot, status, and last-error. `Key` is a stable string for registry lookup and a future the persistence layer persistence id.
- `RoslynRepl.Editor.Patches.PatchRegistry`: in-memory registry keyed by spec key, fires a `Changed` event on every add/update/remove/clear. the engine is intentionally non-persistent — the persistence layer (later) will layer EditorPrefs/asset persistence on the same API.
- `RoslynRepl.Editor.Patches.PatchEngine` + `PatchCodeGenerator` + `HarmonyBridge`: takes a spec, generates a wrapper class containing the user's body plus `__get<T>` / `__set` / `__call<T>` local helpers for private member access, compiles via Roslyn against `AssemblyReferenceCache`, and installs a Harmony `Prefix` that runs the body and skips the original method. Apply / Revert / RevertAll reflect status back into the registry.
- HarmonyBridge talks to `0Harmony.dll` exclusively through reflection so the package compiles even when Harmony isn't installed yet — first-Apply surfaces a single user-facing message ("Harmony is not loaded — Tools / Roslyn REPL / Install Roslyn DLLs installs Harmony alongside Roslyn") instead of breaking import.
- `Tools / Roslyn REPL / Patch Method…` opens the main REPL window and flips its lower pane to a new **Patches** mode tab (paired with the existing **Output** tab). Same pixels, different content — no separate popup window. The Patches view ships the form (target / method / parameter types / body), Apply / Revert / Revert All actions, a status line, and an active-patches list with status dot, Load, and per-row Revert.
- `Tools/Roslyn REPL/Install Roslyn DLLs` now also installs Harmony in the same flow. The previous standalone `Install Harmony` menu is removed; `install-harmony.ps1` still ships under `Tools~/` for manual / re-installation use.
- `RoslynRepl.Editor.Diagnostics.FirstRunPrompt` (`[InitializeOnLoad]`): on the first Editor session after package import, an `EditorUtility.DisplayDialogComplex` with **Install Now / Don't Show Again / Later** offers to download both dependencies in one click. SessionState gates the prompt to once per session; project-scoped EditorPrefs holds the "don't show again" decision so two projects on the same machine can decide independently.
- `Verify Setup` gains an `[optional] Harmony (Runtime Method Patch)` line — `present` once `0Harmony` loads, `absent` plus install-menu pointer otherwise.

### Limitations (intentional)
- **Void instance methods only.** Static methods, methods with non-void return types, `ref` / `out` / `in` parameters, generic methods, and constructors are not patchable in the initial release. Apply throws a specific diagnostic for each so users know what's gated.
- **No persistence.** Patches survive until the next domain reload (Editor restart, script recompile, or Play Mode toggle on default settings). the persistence layer adds EditorPrefs persistence + auto-reapply.
- ~~**Helper-style private member access.** Patch bodies reach private fields/methods through `__get<T>("name")`, `__set("name", value)`, `__call<T>("name", args)` rather than the original-source-style `hp -= amount`.~~ **Resolved in the rewriter.** Both styles work — patches written against helpers explicitly compile clean on the first pass and skip the rewriter; patches written in natural source-style (or pulled from Pull's `.cs` source) get the inaccessible names auto-redirected through the same helpers.
- **No source export.** source-export adds the diff between the patch body and the original `.cs` source plus an "Apply To File" path; until then patches are purely runtime.

### Added (Phase 11 — watch & browser polish)
- `WatchSettings` (new, Editor/Core): per-project boolean `FallbackEnabled` (default `true`) controlling whether `WatchEvaluator` falls back to the `_` carry-over and global instance pool when an expression doesn't compile. Plumbed into `WatchEvaluator.EvaluateOne` so failures stay failed when the user wants compile-only behavior. UI control: a new compact `Fallback` `Toggle` in the watch panel header; flipping it writes through to `WatchSettings` and re-runs `Refresh()` so rows reflect the new resolution rule immediately.
- Persistent "↳ Resolved from: …" line under every fallback Watch row (`rr-watch-row-source`). Phase 10b's tooltip + dim expression label was easy to miss; the row-width breadcrumb is read without hover. Failed rows skip the line so it doesn't clash with the existing red error text.
- `GlobalSearchMaxEntries` lowered from `int.MaxValue` to `1000`. `TryEvaluateGlobalPath` returns an `out bool capHit`; when the cap engages, `SourceDescription` gains an action-phrased hint (`— search capped at 1000; qualify the owner (TypeName.Path) if this is wrong`).
- Object Browser rows now carry per-category modifier classes (`rr-browser-row--mb`, `--so`, `--singleton`) and USS paints a 3px left bar in a per-category color (blue / orange / purple) plus a tinted type cell on SO and Singleton rows. Phase 8's mixed All view was hard to scan; color reads faster than text. Plain rows render unchanged.
- `SetupVerifier`'s console output now ends with two diagnostic lines: live count of `ReplDynamic_*` assemblies (the per-Run dynamic assemblies that can't unload until a domain reload) and the size of the metadata reference cache. Surfaces the slow drift that was previously invisible to users tuning Watch panels with N expressions × M Runs.

### Fixed (Phase 11 — Reset polish)
- `Tools / Roslyn REPL / Reset Project Data` now also clears the Output panel of every live `RoslynReplWindow` instance via the new `ClearOutputAfterReset()` method. Previously the Reset dialog claimed "everything was reset" while the previous run's logs / result tree / summary stayed on screen — a contradiction that was particularly bad for the security-cleanup case. The duration label is blanked, gutter error markers are cleared, and the Ready prompt re-renders so the panel reads idle rather than mysteriously empty.

### Documentation (Phase 11)
- README "Watch Panel" section gains two subsections: a three-step **Resolution order** description (compile → `_` carry-over → global instance pool, capped, with the `Fallback` toggle as the opt-out) and a six-row **Property getter policy** table mapping each surface that might invoke a user property getter to Yes/No with rationale. Users can answer "will this Watch fire side effects?" from one table.
- README "Persistence Model" gains a **Reset scope** table: five rows of what `Reset Project Data` clears (snippets / history / watches / usings / `_` carry-over / Output panel) and four rows of what it explicitly does not touch (scenes / packages / sibling projects / loaded dynamic assemblies). Pairs with the "Verify Setup → diagnostic lines" addition so users know where to read the live dynamic-assembly count.

### Added (Phase 10 — side-effect mitigations)
- `Tools / Roslyn REPL / Reset Project Data` menu wipes all four `EditorPrefs`-backed stores (`SnippetStore`, `RunHistoryStore`, `WatchStore`, `UsingsStore`) for the current project plus the in-memory `_` carry-over. Closes the gap where uninstalling the package or moving on from a project left the user's data behind in the registry — relevant for both housekeeping and for security (history/snippets/watches are stored as plain text and may contain auth tokens, server URLs, or account values from debugging sessions). Snapshots counts before clearing and surfaces them in both the confirmation prompt ("delete X snippets, Y history entries, …") and the result dialog so users see exactly what's gone. No-ops with a friendly dialog when all four stores are already empty.
- `UsingsStore.Clear` and `SnippetStore.Clear` added so the menu has a single uniform API across all four stores (`RunHistoryStore` and `WatchStore` already shipped Clear in Phase 5b/6c). All four fire their `Changed` events so any open popup re-renders to the now-empty state.

### Fixed (Phase 10 — side-effect mitigations)
- `WatchEvaluator.BuildTree` now passes `IncludeProperties = false` to `SimpleObjectSerializer.ToTree`. The result of every Watch refresh used to walk all readable properties of the returned object, calling user-defined getters once per row per Run. Getters in the wild routinely lazy-init, log, mutate counters, or perform IO — pinning a single bad watch could amplify those side effects to *N rows × every Run × every Editor session*. Watch trees still expand into fields, dictionary entries, and IEnumerable items; only property walk on the result is suppressed. The Output panel's `return X;` rendering still walks properties because that's a one-shot user-initiated inspection, not a per-Run loop. Verified via a counter-getter test object: Output path triggers the getter, Watch path triggers it 0 times.
- `WatchResult` gains a `SourceDescription` field, populated whenever the global-search fallback resolves an expression that didn't compile cleanly. Compile-success rows leave it null because their result is unambiguous. The fallback's `_` carry-over branch tags rows with `previous result (\`_\`)`; the global instance sweep tags rows with the matching entry's display + type + sub-label, e.g. `TitleScene (Anipang3Login) — Title` — so common-name fields like `count`, `items`, `_data` no longer leave the user guessing which manager they came from. The expression cell renders italic + dimmer for fallback rows (new `rr-watch-cell-expr--global` modifier class), and both expression and value tooltips include the source string. `TryEvaluateGlobalPath` was extended with an `out InstanceEntry matched` parameter so the resolver can hand the picked entry back without relying on indirection through static state.
- `AssemblyReferenceCache` warms up its metadata reference list on a background thread one Editor frame after startup. Previously the first user Run paid for the full `AppDomain.GetAssemblies` walk + `MetadataReference.CreateFromFile` per assembly — a 400–500ms hang on a typical Unity 6 project (~390 references). Warm-up is best-effort: failures are swallowed, the cache invalidates on `AssemblyLoad`, and the existing lazy-build path inside `GetReferences` remains as a fallback. The defer-by-one-frame avoids extending Editor startup; the background thread avoids blocking the main thread regardless. Verified: cold `GetReferences` measures 476ms / 390 refs, warm read 0ms — that's the wall time the user no longer experiences on their first Run after opening the Editor.

### Documentation (Phase 10)
- README "Persistence Model" gains a sentence about the new `Reset Project Data` menu and explicitly states it never touches data for other Unity projects on the same machine.
- New "Security and Data Handling" section covers the four risks worth knowing before pasting things into snippets or watches: plain-text `EditorPrefs` storage (with platform-specific paths so users know exactly where it lives), arbitrary Editor code execution (the REPL is a power tool — only run trusted snippets), Watch side-effects (re-evaluation amplifies any side effects in the expression itself), and Editor hang from non-cooperative loops (soft cancellation only works when the snippet observes `ct`). Includes a copy-pasteable cooperative-loop pattern for users writing long-running snippets.
- README "Menus" lists the new `Reset Project Data` menu and explains when to reach for it (stale or sensitive entries, decommissioning, machine handoff).

### Changed (Phase 9 — discoverability rename)
- `displayName` rebranded `Roslyn REPL` → `Unity Roslyn REPL` so OpenUPM and Package Manager UI searches for the literal `unity` keyword surface this package. The C# namespaces (`RoslynRepl.Editor.*`), assembly definition, menu paths, and on-disk source layout stay unchanged.
- GitHub repository renamed `ROSLYN-REPL` → `UNITY-ROSLYN-REPL` for the same discoverability reason. All `repository.url`, `documentationUrl`, `changelogUrl`, `licensesUrl` references in `package.json`, README installation snippets (Git URL options, both `main` and tagged), and the OpenUPM descriptor point at the new URL. GitHub auto-redirects from the old slug, so existing clones, PR links, and outbound references keep resolving.
- `unity` added to `package.json: keywords` and to the OpenUPM `topics` list.

### Added (Phase 7 — distribution)
- `RoslynRepl.Editor.Core.DefaultSnippets`: bundled starter library of 8 snippets covering the most common Unity inspection scenarios — Unity version, editor time, active scene, root GameObjects, singleton lookup, memory snapshot, current Selection, and a `_` carry-over example. `ImportAll()` adds them to `SnippetStore`, skipping any name the user already has so re-running the import never overwrites edits the user made to a default.
- `Tools / Roslyn REPL / Import Default Snippets` menu invokes the import and surfaces an `EditorUtility.DisplayDialog` summary (`X added, Y skipped`). If the Snippets popup is open, `SnippetLibraryWindow.NotifyChanged()` refreshes its list immediately.
- README expanded with Quick start, Window guide (ASCII layout + toolbar / side-panel reference tables), Inside snippets (`_` and `ct` accessors), Keyboard shortcuts table, Menus list, and a Troubleshooting section covering six recurring friction points. Roadmap marks Phase 7 shipped; OpenUPM submission moves to its own "Open" section since it's an external workflow rather than a code change.
- `package.json` metadata filled out for OpenUPM submission: `description` rewritten to cover the full Phase 1–7 scope, `author.url`, `repository`, `documentationUrl`, `changelogUrl`, `licensesUrl`, and the `keywords` list expanded with `watch` and `snippet`. Version bumped `0.1.0` → `0.7.0` to mark the first release that ships the planned feature set; semver stays sub-1.0 so a future API breakage doesn't violate a stability contract we haven't earned yet.
- `Documentation~/openupm/com.youngchan.roslyn-repl.yml` — the OpenUPM registry descriptor in its final form, ready to copy into `openupm/openupm/data/packages/` as a single-PR submission once the maintainer is ready.
- README "Installation" section restructured into OpenUPM, Git URL, and Add-From-Disk flows so the docs match the package's distribution channels.

### Added (Phase 6 — watch panel)
- `RoslynRepl.Editor.Core.WatchStore`: project-scoped, `EditorPrefs`-backed list of watch expressions. Same base64-per-entry storage as `RunHistoryStore` so any character a user types is safe. De-dupes exact duplicates so accidental double-Enter doesn't add the same row twice.
- `RoslynRepl.Editor.Core.WatchEvaluator` re-evaluates every saved expression independently. Each watch runs through `ReplEngine.Execute` with its own short timeout (`1000ms`) and the host's effective `Usings`, so a slow or hanging watch can't drag the whole panel down. The evaluator keeps a per-expression preview snapshot and flags rows whose preview just changed via `JustChanged`; the first evaluation after an expression is added doesn't flash (no prior snapshot). `WatchResult` carries `Expression`, `Preview`, `TypeName`, `Failed`, `ErrorMessage`, and the change flag — the view layer's full input contract.
- The user input is wrapped as `return <expr>;` automatically, so users type bare expressions like `Manager.Instance.Count`. Snippets that already start with `return` (or include trailing `;`) are kept as-is for users who want full statements.
- `RoslynRepl.Editor.UI.WatchPanelView`: in-window panel mounted under Output (vertical split). Shows expression / value / type per row, plus a `✕` remove and a `+ Add` field (Enter submits) in the header. Rows that just changed get the `rr-watch-row--changed` class for 1.5 seconds, driven by `VisualElement.schedule.Execute` rather than a coroutine. Failed watches render in italic red.
- Layout: the existing `[code | output]` vertical split is now `[code | [output | watch]]`, with the watch pane at 120px fixed and the inner lower-region at 320px fixed (up from 240px) to keep both visible.
- `RoslynReplWindow.Run` calls `_watch?.Refresh()` after `RenderResult`, so every Run reflects fresh values for any side effect the snippet produced.

### Added (Phase 6 — soft timeout / cancellation)
- `ReplOptions.TimeoutMs` (default 5000ms) wires `CancellationTokenSource.CancelAfter` for every `Execute` call. `ReplOptions.ExternalCancellation` lets a caller (e.g. a future Cancel button or a Watch panel doing batch re-evaluation) thread its own token through; the engine links both via `CreateLinkedTokenSource`. `0` or negative `TimeoutMs` disables the timer.
- `ReplEngine.CurrentCancellation` exposes the linked token to the in-flight snippet; the wrapper class adds a `public static System.Threading.CancellationToken ct` property that pulls through, so snippets cooperate with `ct.ThrowIfCancellationRequested()` inside long loops. `ct` is reset to `CancellationToken.None` between runs so a stale reference can't fire later.
- `ReplResultKind.Cancelled` (and `ReplResult.Cancelled(reason, …)`) distinguishes a clean opt-in cancel from a `RuntimeError`. The reason string disambiguates external cancel vs timeout: "Snippet cancelled by external request" vs "Snippet cancelled after 5000 ms timeout". `RoslynReplWindow` renders cancellations as a `⏹` warning line (not red) and the toolbar summary shows "Cancelled".
- The catch-when filter for `OperationCanceledException` accepts both the strict-correct case (token matches) and the common pattern where a snippet manually throws `OperationCanceledException` after observing `ct.IsCancellationRequested` — the surrounding token signal is enough to call it a cancel. Other `OperationCanceledException`s thrown from unrelated tokens still surface as `RuntimeError`.

### Fixed (Phase 6 — PR review feedback)
- Watch evaluations no longer mutate the user-visible `_` carry-over. Previously every watch ran through `ReplEngine.Execute`, which unconditionally updated `LastResult`, so a watch like `"watch-value"` evaluated between the user's `return 41;` and `return _;` would leave `_` pointing at `"watch-value"` instead of `41`. Added an opt-out `ReplOptions.UpdateLastResult` flag (default `true` for backwards compat) and `WatchEvaluator` sets it to `false`, so passive observation can never quietly mutate the user's previous-result state. Verified with the exact PR repro: `return 41;` → watch eval `"watch-value"` → `return _;` now returns `41`.
- `WatchPanelView` is now disposed before being overwritten on `CreateGUI` rebuild, and disposed again in `OnDisable`. Previously `BindControls` constructed a fresh view on every rebuild without dropping the old one, and the old view's static `WatchStore.Changed` subscription leaked forever — every Add/Remove fired N detached panels' refresh handlers and made each watch evaluate N times against the same `WatchStore`. Verified the `WatchStore.Changed` invocation list now stays at 1 across three back-to-back `CreateGUI` calls (was 1→2→3→4), drops to 0 on `OnDisable`, and rises back to 1 on re-mount.

### Phase 6 limitations (intentionally not shipped)
- **`async` / `await` inside snippets.** A first attempt that switched the wrapper signature to `async Task<object>` and `GetAwaiter().GetResult()`'d the task on the Editor main thread reliably deadlocked: most Unity-side awaits post their continuation back to the same main thread that the engine was blocking, so the continuation could never run. Working around this would require rewriting `Execute` as a polling pump on `EditorApplication.update` (with the UI rebuilt around per-frame callbacks instead of a synchronous result), which is out of scope. Snippets that need to observe an async result should `Task.Run` the work themselves and surface a sentinel via `Debug.Log` or the `_` carry-over.
- **Hard-killing a synchronous snippet that ignores `ct`.** `Thread.Abort` is unavailable on Mono / .NET 6+, and the snippet runs on the main thread, so the engine can't unilaterally stop a tight `while(true){}` loop that never checks `ct`. Soft cancel via `ct` is the only safety net; hangs from non-cooperative code still require killing the Editor process.

### Added (Phase 5 — persistence + variable continuity)
- `RoslynRepl.Editor.Core.ReplEngine.LastResult` (and `ResetLastResult()`): the most recent successful, *non-null* return value from `Execute`. Synthetic `return null;` fallbacks (snippets without an explicit return) and explicit `return null;` no longer overwrite the carry-over, so a debug-print run won't erase the value the user is iterating on.
- `ReplCodeWrapper` exposes a static property `_` on the generated `__ReplScript` class that pulls through to `ReplEngine.LastResult`. Snippets reference it unqualified (`return _ + 1;`) the same way Python's REPL or ipython surface the previous result. A property — rather than an injected local — avoids the CS0219 "declared but never used" diagnostic when the user doesn't reference it, and lets a user-introduced local `_` shadow it cleanly inside its own scope (`int _ = 99; return _;` still compiles).
- `RoslynRepl.Editor.Core.ProjectScopedPrefs.BuildKey(baseName)`: extracted helper that namespaces an `EditorPrefs` key with a stable FNV-1a hash of `Application.dataPath`. `UsingsStore` was refactored to use it; `RunHistoryStore` and the upcoming snippet library use the same helper, so adding a new persistence bucket is a one-liner instead of duplicating the hash logic.
- `RoslynRepl.Editor.Core.RunHistoryStore`: project-scoped, `EditorPrefs`-backed ring of recently-executed snippets (capacity 50, most-recent-first). `Push` de-dupes against the head so re-running an identical snippet doesn't churn the ring; older identical entries are kept since they record real moments the user ran something different in between. Entries are base64-encoded before being joined with `\n` so newlines, commas, and quotes inside user code never corrupt the storage format.
- `RoslynRepl.Editor.UI.RunHistoryWindow`: utility popup with a virtualizing `ListView` of history rows (first line as preview + italic "N lines • M chars" meta). Double-click loads the snippet into the host editor through a static `OnSnippetChosen` event; a `Clear` button (with confirmation dialog) wipes the project's history.
- Toolbar: new `History…` button next to `Usings…`. `RoslynReplWindow.Run` now calls `RunHistoryStore.Push(code)` after `ReplEngine.Execute` returns — including failures, since users often want to scroll back to a broken run, fix the error, and retry.
- `RoslynRepl.Editor.Core.SnippetEntry` + `SnippetStore`: project-scoped library of *named* snippets. `Save(name, code)` upserts in place (preserving order on overwrite); `Delete`, `Rename`, and `Exists` round out the CRUD. Storage encodes each row as `base64(name) + "|" + base64(code)` joined with `\n`, so neither names nor code can corrupt the format regardless of what the user typed.
- `RoslynRepl.Editor.UI.SnippetLibraryWindow`: utility popup with a virtualizing `ListView` (per-row Load / Rename / ✕ buttons), a save form below the list (text field + Save button + overwrite confirmation), and a tiny modal `SimpleStringPrompt` for Rename. The popup stays passive about the host buffer — the host pulls live code at commit time via `OnSaveRequested`, so the popup can never go stale if the user keeps typing after opening it. `OnSnippetChosen` (mirroring `RunHistoryWindow`) loads the chosen code back into the editor.
- Toolbar gains a `Snippets…` button, slotted next to `History…`.

### Fixed (Phase 5 — PR review feedback)
- `_` is now typed `dynamic` instead of `object` on the wrapper class, so operators and member access bind at runtime against the live value: `return _ + 1;` works directly after `return 41;`, no user-side cast required. Previously the static `object` typing forced CS0019 because `+` doesn't exist on `object` × `int`. `AssemblyReferenceCache.BuildReferences` force-loads `Microsoft.CSharp` (the C# runtime binder) before snapshotting the AppDomain — without that step the binder lives in a not-yet-loaded assembly and the dynamic operator dispatch fails to compile.
- `RunHistoryWindow` now subscribes to `RunHistoryStore.Changed` in `OnEnable` (and unsubscribes in `OnDisable`), so an open popup refreshes its `ListView` whenever the host window pushes a new entry. Previously the popup snapshotted the store at mount and silently went stale: running snippets while the popup was open updated the backing store but not the visible list. Verified: with the popup already open, three sequential `Push` calls move both `store.Count` and `list.itemsSource.Count` from 0→1→3 in lockstep, with the head row showing the most recent entry.

### Added (Phase 4 — editor UX polish)
- `RoslynRepl.Editor.UI.CodeEditorView`: composite VisualElement that wraps the multiline `TextField` with a sibling line-number gutter and a footer caret-position indicator (`Ln N, Col M`). Replaces the bare `TextField` previously bound to `code-input`.
- The gutter pools its row VisualElements: existing rows are reused on every keystroke and only the delta of new / removed lines is added or removed, so typing in long buffers doesn't churn the UI tree.
- Caret indicator updates on `KeyUpEvent`, `MouseUpEvent`, and `FocusInEvent` — sufficient for a status display without polling the cursor every frame.
- New USS classes (`rr-code-editor`, `rr-code-row`, `rr-code-gutter`, `rr-code-gutter-row`, `rr-code-gutter-marker`, `rr-code-gutter-number`, `rr-code-input`, `rr-code-caret`) styled to match the existing dark theme. Gutter row height (16px) matches the TextField's natural 12px line height for one-to-one alignment.
- `CodeEditorView.SetErrorMarkers` / `ClearErrorMarkers` API renders red gutter dots with hover tooltips for compile diagnostics. `RoslynReplWindow.RenderResult` clears markers at the start of every run and sets them from `result.Diagnostics` (user-code only — internal wrapper-region diagnostics are omitted because they have no meaningful line in what the user typed). The user's next keystroke also clears stale markers via `CodeEditorView`'s own value-changed callback.
- `RoslynRepl.Editor.Core.UsingsStore`: `EditorPrefs`-backed persistence for the user's *additional* using namespaces. `LoadCustom` returns the trimmed/de-duplicated user list, `Save` writes it (and fires a `Changed` event), `EffectiveUsings` concatenates defaults with the user list in stable order. Storage is a single comma-joined string under `RoslynRepl.CustomUsings`.
- `RoslynRepl.Editor.UI.UsingsEditorWindow`: utility popup (`ShowUtility`) listing the read-only defaults at top, then an editable list of the user's additions with a per-row `✕` remove button, then a single text field + `+ Add` button (Enter also submits). Pasted entries are normalized — `using Foo;` and `Foo` both end up as `Foo`. Saves on every mutation; new entries take effect on the next Run because `RoslynReplWindow.Run` always fetches `UsingsStore.EffectiveUsings()` fresh per call.
- Toolbar: new `Usings…` button between the existing `Verify Setup` and the rest of the row.

### Fixed (Phase 4 — PR review feedback)
- `UsingsEditorWindow.CreateGUI` now clears `rootVisualElement` before mounting its layout. `CreateGUI` is not a one-shot hook — panel rebuilds and domain-reload recovery can fire it again on the same window — and the previous code appended a fresh control tree on top of the old one each time, leaving duplicated sections and stale `_customList` references in the older copies. Verified: three back-to-back `CreateGUI()` invocations now leave `rootVisualElement.childCount` at a steady 6, where it previously climbed by 6 every call.
- `UsingsStore` now namespaces its `EditorPrefs` key with a stable FNV-1a hash of `Application.dataPath`, so each Unity project on the machine gets its own bucket. Previously every project on the same user account shared a single `RoslynRepl.CustomUsings` key, which meant adding a project-specific namespace like `MyGame.Runtime` in one repo would silently inject the same `using` into every other Unity project that imported the package, firing CS0234/CS0246 until the user discovered and removed it. The hash uses FNV-1a rather than `string.GetHashCode` because modern .NET runtimes randomize string hashes per process — `GetHashCode` would change the storage location on every editor restart and silently lose the user's saved usings. A one-shot migration moves any leftover value from the old global key into the current project's bucket and then deletes the global key so a different project opening the package next doesn't inherit it; if both keys exist (ambiguous which project the legacy value belonged to), the per-project value wins and the legacy key is discarded.

### Phase 4 limitations (intentionally not shipped)
- Inline C# syntax highlighting in the code editor. UI Toolkit `TextField` paints text with a single foreground color and has no per-token color API; the workarounds (RichText overlay or parallel non-editable highlight surface kept in sync on every keystroke) add substantial complexity for a quality-of-life nicety in a snippet REPL. Diagnostics are already surfaced via the gutter markers added above and in the output panel. See README "Known limitations" for revisit conditions.

### Added (Phase 3 — instance browser side panel)
- New `RoslynRepl.Editor.Core.InstanceLocator` enumerates user-visible runtime instances by category (`MonoBehaviour`, `ScriptableObject`, `Singleton`, `All`) with substring filtering on type/display name. Hides Unity-shipped types, Editor framework objects, and the package's own assembly so the list stays focused on the user's project.
- `SingletonScanner` (`[InitializeOnLoad]`) reflects over user assemblies for static `Instance` properties / fields. Member discovery is cached for the domain lifetime (invalidated on `AssemblyLoad`); values are read fresh each call so destroyed singletons drop out.
- `RoslynRepl.Editor.UI.ObjectBrowserView` is a UI Toolkit side panel: title row + refresh button, `EnumField` category drop-down, `ToolbarSearchField`, and a virtualizing `ListView` of results. Each row shows display name (bold), short type name (italic teal), and a sub-label (scene / "ScriptableObject" / "Singleton"); inactive rows render dimmer.
- The window now lays out as `[toolbar] / [browser | code+output]` via an outer horizontal `TwoPaneSplitView` (browser fixed at 280px) wrapping the existing vertical split.
- Double-clicking (or Enter on) a browser row renders that instance into the output panel through `SimpleObjectSerializer.ToTree`, equivalent to typing `return X;` — but no code required.

### Fixed (Phase 3 — Play Mode safety, surfaced while browsing real managers)
- `SingletonScanner` now detects **any** self-returning public static member regardless of name (`Instance`, `it`, `I`, `Self`, `Current`, `Singleton`, etc.), so plain C# managers like `LobbyFriendManager.it` are surfaced. Member-type matching accepts both directions: the canonical exact / derived case (`public static Foo Instance` declared on `Foo`) is taken regardless of name, and the base / interface case (`public static IService Current => _instance;` on a class that implements `IService`, or `public static BaseThing Instance = new DerivedThing();` declared on the derived class) is taken when the name is in the standard singleton accessor set — guarding against false positives like `public static IDisposable s_dispose;` on unrelated classes. Compiler-generated closure / lambda-cache types (names starting with `<`) are filtered out so they don't flood the list.
- For plain C# *property* singletons whose getter we still won't invoke (lazy-init side effects), a sibling private static **backing field** of the same type is read directly when present — covering the canonical `private static T _instance; public static T it => _instance ??= new T();` pattern.
- `InstanceEntry.Object` (UnityEngine.Object) → `InstanceEntry.Value` (object). Plain C# instances now flow through the browser and into `ToTree` on double-click.
- `InstanceCategory.All` no longer includes the Singleton sweep — that path scans every loaded user assembly and is too heavy to run automatically. Users opt in by selecting the Singleton category.
- `ObjectBrowserView` no longer auto-`Refresh()`s on construction; the first scan happens only when the user changes category, types in search, or presses ↻. Avoids freezing the Editor on window open or domain reload.
- `SimpleObjectSerializer` hardened against runaway graphs:
  * `System.Delegate` (Action / Func / event) is treated as a leaf — walking the invocation list reaches every subscribed view and explodes the graph. Preview shows handler count and first target.
  * `BuildNode` now threads a `BuildState` (replacing the loose `Options` + `visited` pair) so a per-call **total node cap** (default 2000) can abort cleanly with a `(node cap reached)` leaf.
  * Default `MaxDepth` lowered 6 → 4.
- `ValueFormatter` renders delegate previews like `Action → ClassName.Handler` or `Action (3 targets, first: …)`.

### Added (Phase 2 — object tree view in output)
- `RoslynRepl.Editor.Core.ReplValueNode`: a row in the result tree (`Name`, `TypeName`, `Preview`, `IsExpandable`, `Children`)
- `SimpleObjectSerializer.ToTree(value)`: reflection-based converter that walks fields (incl. private + inherited) and readable instance properties, with cycle detection (reference-equality `HashSet`), depth cap (default 6), collection-head cap (default 50), and special handling for `IDictionary` / `IEnumerable`
- `ValueFormatter`: 1-line previews for primitives, strings (truncated + escaped), Unity types (`Vector2/3/4`, `Color`, `Quaternion`, `Rect`, `Bounds`, `GameObject`, `Component`, `UnityEngine.Object`), collections, and a `ToString` fallback
- `TypeFormatter`: short type names with C# keyword aliases, generic argument formatting, array rank, and `Nullable<T>` → `T?`
- Window output now renders complex results with a `MultiColumnTreeView` (Name / Type / Value columns), auto-expanding the root level. Leaf results (`int`, `string`, `Vector3`, etc.) keep the inline `=> X` rendering from Phase 1 to avoid one-row trees
- Compiler-generated `<...>k__BackingField` entries are filtered out so auto-property values appear only once via the property itself

### Fixed (Phase 2 — surfaced while inspecting real Unity scene objects)
- `ValueFormatter.Format` no longer throws `NullReferenceException` when the value is a Unity "fake-null" reference (a wrapper whose native side is destroyed or was never assigned). Each `UnityEngine.Object` / `Component` / `GameObject` branch now compares against null using Unity's overload first and returns a `(destroyed)` marker.
- `SimpleObjectSerializer.BuildNode` short-circuits on fake-null `UnityEngine.Object` values, emitting a `(missing/destroyed)` leaf instead of attempting to walk a destroyed object's fields.
- `UnityEngine.Transform` (and `RectTransform`) is now treated as a leaf type. Its computed accessors (`position`, `lossyScale`, `eulerAngles`, …) read from internal matrices and fire a native `Assertion failed: 'ValidTRS()'` when the matrix is degenerate; those asserts bypass managed `try/catch` and spam the Console.
- Properties whose **declaring type** lives in a Unity-shipped assembly (`UnityEngine.*`, `UnityEditor.*`, `Unity.*`) are skipped. Many of those accessors (`Image.mainTexture`, `Material.color`, `Renderer.bounds`, `Canvas.worldCamera`, …) read native state and trigger the same class of native asserts. Properties declared on **user types** (MonoBehaviour / ScriptableObject subclasses, etc.) are walked normally, so user-defined `Computed => …` accessors stay visible. Users wanting a specific Unity-shipped computed value can call it directly (e.g. `return rect.localPosition;`).
- `BuildDictChildren` now mirrors `BuildEnumerableChildren`'s try/catch: a custom `IDictionary` whose `GetEnumerator()` / `MoveNext` / `Current` throws no longer aborts the whole `ToTree`; partially-collected entries plus a `<enumeration>` error leaf are returned.
- `ValueFormatter.Format` is now guaranteed not to throw. The dict / collection preview branches read `Count` inside their own `try/catch` and fall back to `(IDictionary, count unavailable)` / `(ICollection, count unavailable)`; an outer `try/catch` around the whole formatter returns `<TypeName> <preview error: …>` for any other escape. This closes a hole where `BuildNode` called `Format(value)` *before* `BuildDictChildren`'s guard could run, so a dict with a throwing `Count` getter still aborted the whole `ToTree`.

### Added (Initial REPL release)
- `RoslynRepl.Editor.Core.ReplEngine`: Roslyn-based one-shot compile + execute pipeline (`CSharpCompilation` → `Emit` → `Assembly.Load` → invoke)
- `ReplCodeWrapper`: wraps user statements in a generated class/method with line-offset tracking so compiler diagnostics map back to user lines
- `ReplLogCapture`: thread-safe `Application.logMessageReceivedThreaded` capture for the duration of a single Execute() call
- `AssemblyReferenceCache` (`[InitializeOnLoad]`): lazy `MetadataReference` cache; auto-invalidates on `AssemblyLoad` so newly compiled user scripts are picked up
- `ReplOptions`: pluggable list of injected `using` namespaces (defaults: System, Linq, UnityEngine, UnityEditor)
- `ReplResult` / `ReplResultKind` / `LogEntry` / `DiagnosticInfo`: result model surfaced to the UI layer
- Window: code editor (UI Toolkit `TextField` multiline) + output panel (`ScrollView` of color-coded `Label`s) split vertically via `TwoPaneSplitView`
- Toolbar: ▶ Run, Clear, duration display, output summary
- Keyboard shortcuts: F5 and Ctrl+Enter to run
- Code input persists across domain reloads via `SessionState`

### Fixed (Phase 1 — PR review feedback)
- Background Play Mode logs (server callbacks, ad SDK, gameplay updates) emitted during compile / emit / load no longer leak into REPL output. `ReplLogCapture` now starts only around the `MethodInfo.Invoke` window, and `ReplEngine` tags each captured log with `LogEntry.FromSnippet` based on whether the generated `__ReplScript` class appears in the stack trace; the UI shows only snippet-originated logs.
- F5 / Ctrl+Enter no longer triggers Run multiple times after a CreateGUI rebuild. The window now unregisters the `KeyDownEvent` handler before re-registering, uses a named `OnPlayModeChanged` method (so subscriptions can be removed), and unsubscribes `EditorApplication.playModeStateChanged` in `OnDisable`.
- Snippets that don't return a value no longer display a synthetic `=> null`. The output panel checks `ReplResult.HasReturnValue` and skips the result line when the wrapper's fallback `return null;` was used.

### Phase 1 limitations (deferred to later phases)
- No variable persistence between runs (each Execute() is isolated) — Phase 4
- No async/await support — Phase 6
- No timeout/cancellation (infinite loops will hang the Editor) — Phase 6
- Result formatting is `ToString()` only; tree view comes in Phase 2

### Added (Phase 0 — package skeleton)
- UPM package layout (`package.json`, asmdef, README, CHANGELOG, LICENSE)
- Empty UI Toolkit `EditorWindow` (`Tools / Roslyn REPL / Open`)
- `Tools / Roslyn REPL / Verify Setup` diagnostic with origin classification (BundledByUs / UnityShipped / NuGetForUnity / OtherPackage / External / Unknown)
- `AssemblyResolutionGuard` for detecting duplicate Roslyn copies (Unity 6.5 readiness)
- `Tools / Roslyn REPL / Install Roslyn DLLs` (and `Tools~/install-roslyn.ps1`) to fetch Roslyn 4.8.0 binaries from NuGet
- Bundled Roslyn DLLs with stable GUIDs and Editor-only Plugin Importer settings (`validateReferences: 1`)

### Verified
- Compiles cleanly on Unity 6 (6000.0.65f1) with zero console errors
- UI Toolkit window mounts UXML/USS, shows version, mode badge, status, and Verify button
- ValidateReferences correctly defers to a host project's existing Roslyn (e.g. NuGetForUnity-installed copy) when versions match — bundled DLLs serve as fallback for projects that have none
